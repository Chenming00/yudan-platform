# 鱼蛋家庭开支平台：多 Agent 开发任务计划

> 执行方式更新（2026-09-01）：按用户要求，后续由同一个 GPT 连续实现并直接交付 `main`，不再创建多个 Agent 分支。文中的 A00-A12 保留为工作包、依赖和验收清单，不再表示不同执行者或代码所有权。

> 项目仓库：<https://github.com/Chenming00/yudan-platform>  
> 总体规划：[计划.md](./计划.md)  
> 文档用途：供多个 AI Agent 并行开发、审查、测试和交付  
> 更新日期：2026-09-01

## 1. 使用方式

任何 Agent 开始工作前必须：

1. 完整阅读 `计划.md` 和本文件；
2. 确认自己的任务编号、依赖和允许修改的目录；
3. 检查目标分支和已合并的前置任务；
4. 不修改其他 Agent 独占的文件；
5. 在提交中包含代码、测试、必要文档和验证结果；
6. 工作完成后按本文件的交接模板汇报。

本计划的目标是让多个 Agent 并行工作，但不让多个 Agent 同时争夺 Prisma Schema、全局配置、共享组件和跨模块事务等高冲突区域。

## 2. 固定架构决策

所有 Agent 必须遵守：

- 主应用：Next.js App Router；
- 语言：TypeScript，开启严格模式；
- 数据库：Supabase PostgreSQL；
- ORM：Prisma；
- 认证：Supabase Auth 邮箱密码；GitHub 作为可选 Identity；
- 注册：邀请码注册，关闭公开注册；
- 超级管理员：`William.chen@utah.edu`；
- 权限：RBAC + 权限码 + `household_id` 数据范围；
- 部署：Vercel；
- 图片与附件：Cloudflare R2；
- 图片优化：Cloudflare Images，按需启用；
- 金额：Prisma Decimal，API 中使用十进制字符串；
- 时区：业务展示使用 `Asia/Shanghai`；
- ID：新业务实体默认 UUID；
- 模块：账本、儿童保健、衣柜、消耗品、成长日志；
- 账本是唯一财务事实来源；
- 业务模块通过 TransactionAllocation 与账本关联；
- 消耗品不向用户暴露批次，数据层保留 StockEntry；
- 消耗出库不重复产生账本支出；
- 关键跨模块写入必须使用数据库事务。

## 3. Agent 协作规则

### 3.1 分支命名

```text
agent/<任务编号>-<简短名称>
```

示例：

```text
agent/A01-bootstrap
agent/A03-auth-rbac
agent/A08-consumables
```

禁止 Agent 直接向 `main` 推送功能代码。每个任务应独立 PR。

### 3.2 提交规则

提交信息：

```text
<type>(<module>): <summary>
```

示例：

```text
feat(ledger): add transaction allocation service
test(auth): cover expired invitation rejection
docs(media): document R2 upload lifecycle
```

每个提交只处理一个逻辑主题。禁止提交真实 Secret、数据库密码、API Key、邀请码或生产数据。

### 3.3 文件所有权

以下区域采用单一负责人：

| 区域 | 唯一负责人 |
|---|---|
| `prisma/schema.prisma` | A02 数据模型 Agent |
| `prisma/migrations/**` | A02 数据模型 Agent |
| 根目录构建配置 | A01 平台 Agent |
| `lib/auth/**` 与权限核心 | A03 认证权限 Agent |
| `modules/ledger/**` | A04 账本 Agent |
| `modules/media/**` | A05 资源 Agent |
| `modules/care/**` | A06 儿童保健 Agent |
| `modules/wardrobe/**` | A07 衣柜 Agent |
| `modules/consumables/**` | A08 消耗品 Agent |
| `application/**` | A09 编排 Agent |
| 历史数据迁移脚本 | A10 数据迁移 Agent |
| 跨模块 E2E 与安全测试 | A11 QA Agent |
| Vercel、CI 和发布文档 | A12 发布 Agent |

功能 Agent 如需修改 Prisma Schema，应提交“Schema 变更请求”给 A02，不得与 A02 并行修改 Schema 和 Migration。

### 3.4 禁止事项

- 不得删除或改写他人未合并的工作；
- 不得使用 `git reset --hard` 覆盖工作；
- 不得在生产数据库运行 `prisma db push`；
- 不得把权限判断只放在前端；
- 不得让页面或 Route Handler 直接调用 Prisma；
- 不得在业务模块中直接访问其他模块 Repository；
- 不得用 Float 保存金额；
- 不得在数据库保存 R2 Presigned URL；
- 不得把私有 R2 Bucket 设置为公开；
- 不得在测试中调用真实生产 OAuth、R2 或数据库；
- 不得在没有回退方案时执行数据迁移。

## 4. 依赖关系

```text
A00 架构契约
        ↓
A01 平台初始化 ──────────┐
        ↓                │
A02 数据模型             │
   ├──────┬──────┬──────┤
   ↓      ↓      ↓      ↓
A03      A04     A05    共享基础
认证     账本     资源
   │       │       │
   ├───────┼───────┐
   ↓       ↓       ↓
A06      A07      A08
保健     衣柜     消耗品
   └───────┼───────┘
           ↓
        A09 跨模块编排
           ↓
        A10 数据迁移
           ↓
        A11 QA / 安全
           ↓
        A12 发布切换
```

可并行范围：

- A03、A04、A05 可以在 A01、A02 和契约冻结后并行；
- A06、A07、A08 可以并行；
- A10 可提前编写只读导出和校验工具，但最终导入需等待目标 Schema；
- A11 可提前搭建测试框架，完整 E2E 等待模块集成；
- A12 可提前配置 Preview，生产切换等待全部 Gate。

## 5. 公共契约

### 5.1 模块公开接口

每个模块只通过 `modules/<module>/index.ts` 导出公开能力。

禁止：

```ts
import { ledgerRepository } from "@/modules/ledger/repositories/private";
```

允许：

```ts
import { ledgerService } from "@/modules/ledger";
```

### 5.2 Service 调用上下文

所有写服务接收：

```ts
export interface ActionContext {
  userId: string;
  householdId: string;
  requestId: string;
  idempotencyKey?: string;
}
```

权限检查使用：

```ts
authorize({
  userId,
  householdId,
  permission,
  resourceId,
});
```

### 5.3 金额

内部：

```ts
Prisma.Decimal
```

API：

```json
{
  "amount": "123.45"
}
```

禁止 API 返回 JavaScript 浮点金额。

### 5.4 API 响应

成功：

```json
{
  "success": true,
  "data": {},
  "requestId": "..."
}
```

失败：

```json
{
  "success": false,
  "error": {
    "code": "PERMISSION_DENIED",
    "message": "没有权限执行此操作"
  },
  "requestId": "..."
}
```

错误码至少包括：

- `AUTH_REQUIRED`
- `REGISTRATION_INVITE_REQUIRED`
- `INVITATION_INVALID`
- `PERMISSION_DENIED`
- `RESOURCE_NOT_FOUND`
- `VALIDATION_FAILED`
- `CONFLICT`
- `IDEMPOTENCY_CONFLICT`
- `INTERNAL_ERROR`

### 5.5 日期

- 日期：`YYYY-MM-DD`；
- 时间：ISO 8601 UTC；
- 展示：`Asia/Shanghai`；
- 禁止依赖服务器本地时区；
- 数据库使用 `timestamptz` 对应 Prisma DateTime。

### 5.6 审计

敏感操作调用统一 Audit Service，禁止各模块自定义不兼容日志结构。

## 6. 工作包总览

| 编号 | Agent | 工作包 | 依赖 | 主要交付 |
|---|---|---|---|---|
| A00 | 架构 Agent | 契约冻结 | 无 | ADR、接口、命名规则 |
| A01 | 平台 Agent | 项目初始化 | A00 | 可构建 Next.js |
| A02 | 数据模型 Agent | Prisma 与数据库 | A00、A01 | Schema、Migration、Seed |
| A03 | 认证权限 Agent | 邀请、登录、RBAC | A02 | Auth、权限、管理后台 |
| A04 | 账本 Agent | 账本核心 | A02 | Transaction、统计、API |
| A05 | 资源 Agent | Cloudflare R2 | A02、A03 接口 | 上传、访问、删除 |
| A06 | 保健 Agent | 儿童保健 | A02、A03、A04、A05 | 保健记录与开支 |
| A07 | 衣柜 Agent | 衣柜 | A02、A03、A04、A05 | 衣物与购衣 |
| A08 | 消耗品 Agent | 库存 | A02、A03、A04、A05 | Pantry 迁移 |
| A09 | 编排 Agent | 组合记账 | A04、A06、A07、A08 | 跨模块事务 |
| A10 | 迁移 Agent | 历史数据 | A02、A04、A06、A08 | 导入和校验 |
| A11 | QA Agent | 测试与安全 | A03-A10 | E2E、安全报告 |
| A12 | 发布 Agent | CI/CD 与切换 | A01、A11 | Preview、生产发布 |

## 7. A00：架构契约 Agent

### 目标

在业务开发开始前冻结高冲突契约。

### 允许修改

- `docs/adr/**`
- `docs/contracts/**`
- `modules/*/types.ts` 的初始接口草案
- 本计划中的契约补充

### 任务

- ADR-001：模块化单体；
- ADR-002：账本作为财务事实来源；
- ADR-003：TransactionAllocation 拆分；
- ADR-004：邀请注册与 RBAC；
- ADR-005：R2 公私 Bucket；
- ADR-006：Vercel 主应用 + Cloudflare 资源；
- 定义 ID、金额、时间、错误和分页规范；
- 定义模块公开 Service 接口；
- 定义 Schema 变更请求模板；
- 定义审计事件命名。

### 验收

- 所有模块接口不存在循环依赖；
- 金额、时间和错误格式明确；
- A01-A09 可以基于契约独立开发；
- 未实现业务代码。

## 8. A01：平台初始化 Agent

### 目标

建立可构建、可测试、可 Preview 的项目骨架。

### 允许修改

- 根目录配置；
- `app/layout.tsx`
- `app/globals.css`
- `components/layout/**`
- `components/navigation/**`
- `components/ui/**`
- `lib/config/**`
- `.github/workflows/**`

### 任务

- 初始化 Next.js App Router + TypeScript strict；
- 配置 Tailwind CSS、Radix UI、Lucide；
- 配置 ESLint、格式化和路径别名；
- 配置单元测试和 Playwright；
- 建立模块目录；
- 建立桌面和移动导航；
- 建立错误页、Loading、404；
- 创建 `.env.example`，只写变量名；
- 配置 Vercel Preview；
- 建立 CI：lint、typecheck、test、build。

### 验收

```text
npm run lint
npm run typecheck
npm run test
npm run build
```

全部通过，Preview 可访问。

## 9. A02：数据模型 Agent

### 目标

建立所有模块共享的数据库基础，并作为 Prisma 唯一负责人。

### 独占修改

- `prisma/schema.prisma`
- `prisma/migrations/**`
- `prisma/seed.ts`
- `lib/db/**`

### 任务

建立：

- AppUser；
- Household；
- HouseholdMember；
- Role；
- Permission；
- RolePermission；
- UserGlobalRole；
- Invitation；
- RegistrationIntent；
- ApiCredential；
- AuditLog；
- IdempotencyKey；
- Transaction；
- TransactionAllocation；
- Category；
- PaymentAccount；
- BabyProfile；
- GrowthRecord；
- VaccineCatalog；
- VaccineRecord；
- CareRecord；
- WardrobePurchase；
- WardrobeItem；
- Product；
- StockEntry；
- InventoryLog；
- ConsumablePurchase；
- ConsumablePurchaseItem；
- MediaAsset；
- MediaLink。

其他要求：

- 全部主要业务表包含 `household_id`；
- 金额字段使用 Decimal；
- 添加唯一约束、外键和必要索引；
- Seed 创建系统角色和权限；
- Seed 不创建真实生产用户；
- 为超级管理员引导提供幂等函数或事务；
- 生成 ERD；
- 编写 Migration 回退说明。

### 验收

- 空数据库可执行全部 Migration；
- 重复执行 Seed 不产生重复角色；
- Prisma Client 正常生成；
- 索引覆盖常用时间、家庭和来源查询；
- 数据库约束能阻止 Allocation 合计之外的基础非法数据；复杂合计由 Service 保证；
- Schema 不包含真实邮箱以外的 Secret。

## 10. A03：认证、邀请与权限 Agent

### 目标

完成关闭公开注册的邀请码系统和服务端权限系统。

### 允许修改

- `app/(auth)/**`
- `app/invite/**`
- `app/admin/users/**`
- `app/admin/invitations/**`
- `app/api/auth/**`
- `app/api/admin/users/**`
- `app/api/admin/invitations/**`
- `modules/auth/**`
- `modules/permissions/**`
- `lib/auth/**`

### 任务

- Supabase 邮箱密码注册与登录；
- Before User Created Hook，拒绝无 RegistrationIntent 的 signup；
- `auth.users` 插入触发器原子创建平台用户、家庭成员并消费邀请；
- 邮箱确认、找回密码和自定义 SMTP 接口；
- GitHub Identity 可选绑定，不作为注册前提；
- 超级管理员首次引导；
- 超级管理员邮箱标准化校验；
- 创建、验证、消费、撤销邀请码；
- 短时一次性 RegistrationIntent；
- AppUser 状态检查；
- RBAC 和权限码；
- household 数据范围；
- API Key Scope 验证；
- 权限缓存与主动失效；
- 用户停用；
- 角色变更；
- 邀请和权限审计；
- 管理后台；
- 限流接口抽象。

### 关键测试

- 无邀请码的新用户注册失败；
- 直接绕过应用调用 Supabase signup 失败；
- 有效邀请码注册成功；
- 邀请只能使用一次；
- 过期和撤销邀请失败；
- 已注册用户再次登录不需要邀请；
- 未绑定 GitHub 的用户可以正常注册和登录；
- 未确认邮箱的用户不能访问业务数据；
- 被停用用户无法进入；
- VIEWER 不能写入；
- OWNER 不能授予 SUPER_ADMIN；
- 跨家庭访问被拒绝；
- 前端伪造邮箱无法成为超级管理员。

### 验收

所有授权发生在服务端；直接调用 API 不能绕过权限。

## 11. A04：账本核心 Agent

> 实现状态（2026-09-01）：已完成统一账本服务、跨模块拆分、收入/支出/退款、并发退款保护、软删除、审计、筛选统计、游标分页、shadcn/ui 页面、REST API 和旧账本兼容入口；待生产数据库完成备份 Gate 后部署 Migration。

### 目标

建立全平台唯一财务事实来源。

### 允许修改

- `app/ledger/**`
- `app/api/ledger/**`
- `modules/ledger/**`
- `components/ledger/**`

### 任务

- 收入、支出、退款；
- Transaction CRUD；
- TransactionAllocation；
- 普通快速记账；
- 一笔账拆分多个模块；
- 分类、支付方式、商户和备注；
- 月度、日度和分类统计；
- 游标分页、搜索和筛选；
- 退款关联；
- 软删除和审计；
- Decimal API 序列化；
- 旧账本 API 兼容接口；
- LedgerService 公共接口。

### 公共接口

至少导出：

```ts
createTransaction()
updateTransaction()
createRefund()
getTransaction()
listTransactions()
getLedgerSummary()
```

### 验收

- Transaction 金额等于 Allocation 合计；
- 统计不重复计算；
- 分页不遗漏、不重复；
- 退款保留原始账目；
- 数据范围和权限测试通过。

## 12. A05：Cloudflare 资源 Agent

> 实现状态（2026-09-01）：已完成 R2 S3 存储适配、短时直传与下载签名、大小/MIME/扩展名/文件头校验、PENDING→READY 确认、家庭授权、MediaLink、逻辑删除、物理删除重试、Vercel Cron 清理入口、上传组件和部署文档；待配置真实 R2 Bucket、CORS 和 Vercel Secret 后联调。

### 目标

完成 R2 公私资源上传、读取、关联和删除。

### 允许修改

- `app/api/media/**`
- `modules/media/**`
- `components/media/**`
- `lib/cloudflare/**`
- `docs/cloudflare/**`

### 任务

- MediaStorage 接口；
- R2 S3 实现，`region=auto`；
- 公私 Bucket；
- Presigned PUT URL；
- PENDING → READY 确认流程；
- HEAD 校验；
- MIME、扩展名、Magic Bytes 和大小校验；
- MediaAsset、MediaLink Service；
- 私有下载授权；
- 公开资源 URL；
- 逻辑删除与后台物理清理；
- 上传过期清理；
- CORS 文档；
- Cloudflare Images 变体接口预留；
- 测试使用 InMemoryMediaStorage 或 S3 Mock。

### 验收

- 文件不经过 Vercel 正文代理即可上传；
- 私有资源不能匿名访问；
- 跨家庭访问失败；
- 数据库不保存 Presigned URL；
- 删除失败可重试；
- Secret 不进入客户端 Bundle。

## 13. A06：儿童保健 Agent

> 实现状态（2026-09-01）：已完成宝宝档案、生长记录、疫苗目录/接种记录、儿保记录、服务端权限校验、家庭隔离、审计及 REST API；付费记录通过 `CHILD_CARE` Allocation 关联，待 A09 组合记账统一编排。

### 目标

迁移并扩展体重、疫苗、儿保、就诊、药品和保健品记录。

### 允许修改

- `app/care/**`
- `app/api/care/**`
- `modules/care/**`
- `components/care/**`

### 任务

- BabyProfile；
- 体重、身高、头围；
- 生长趋势；
- VaccineCatalog；
- VaccineRecord；
- 儿保、就诊、药品、保健品记录；
- 免费记录；
- 付费记录与 Allocation 关联；
- 医疗附件关联 MediaAsset；
- 服务端健康权限；
- 旧健康 API 兼容。

### 验收

- 免费疫苗不产生支出；
- 自费疫苗可关联账本；
- 同日和同计划唯一约束正确；
- 未授权用户无法读取健康数据；
- 历史健康数据显示一致。

## 14. A07：衣柜 Agent

> 实现状态（2026-09-01）：已完成衣物档案、购买与批量生成、赠送、状态流转、出售收入关联、退款保留规则、私有图片关联、家庭权限隔离、页面和 REST API；一次性组合记账待 A09 编排。

### 目标

建立儿童衣物及购衣开支模块。

### 允许修改

- `app/wardrobe/**`
- `app/api/wardrobe/**`
- `modules/wardrobe/**`
- `components/wardrobe/**`

### 任务

- 衣物名称、品类、尺码、季节；
- 状态流转；
- 购买记录；
- 批量生成衣物；
- 赠送录入；
- 图片关联；
- 退款、出售和收入关联；
- 分类和统计；
- WardrobeService 公共接口。

### 验收

- 购买可关联账本；
- 赠送不产生支出；
- 退款保留购买记录；
- OWNER/EDITOR/VIEWER 权限正确；
- 衣柜图片为私有资源。

## 15. A08：消耗品 Agent

### 目标

迁移 Pantry 核心功能，并将显式批次改为内部 StockEntry。

### 允许修改

- `app/consumables/**`
- `app/api/consumables/**`
- `modules/consumables/**`
- `components/consumables/**`

### 任务

- 产品管理；
- 当前库存聚合；
- 采购入库；
- 赠送、转入、盘点和历史补录；
- 自动先到期出库；
- 库存日志；
- 撤销；
- 低库存、缺货、临期和过期；
- 补货清单；
- 产品组；
- 图片关联；
- 采购与 Allocation 关联；
- 旧 Pantry API 和 Agent Skill 兼容。

### 约束

- UI 不要求用户输入批次；
- StockEntry 保留到期日和成本；
- 出库不创建账本支出；
- 已有库存操作不可因账本失败产生半完成状态。

### 验收

- 迁移前后每个产品库存一致；
- 自动出库顺序正确；
- 并发出库不产生负库存；
- 采购入库可关联账本；
- 非采购入库不产生账目。

### 实施状态（2026-09-01）

已完成。产品、当前库存聚合、采购/非采购入库、FEFO 出库、盘点、日志、30 秒撤销、补货建议、产品组、私有图片与旧 Pantry 兼容 API 已落地。日常 UI 不要求内部库存编号；采购严格关联 `CONSUMABLES` 支出 Allocation。A08 复用保留的 `yudan-wupin` 结构，未直接执行生产数据库迁移。

## 16. A09：跨模块编排 Agent

### 目标

实现一次付款同时创建多个模块记录。

### 独占修改

- `application/**`
- `app/ledger/new/**`
- `modules/dashboard/**`
- `app/page.tsx`

### 依赖

A04、A06、A07、A08 公共 Service 接口稳定。

### 任务

- ExpenseOrchestrator；
- RefundOrchestrator；
- 组合记账页面；
- 跨模块 Prisma Transaction；
- 幂等键；
- 一次付款多 Allocation；
- 消耗品、衣柜、保健记录编排；
- 任一步失败全部回滚；
- 家庭总览；
- 模块支出对比；
- 最近事件聚合。

### 验收

示例必须通过：

```text
京东付款 ¥520
├── 消耗品 ¥300
├── 衣柜 ¥150
└── 儿童保健 ¥70
```

产生一条 Transaction、三条 Allocation 和对应业务记录，总支出只能统计为 ¥520。

### 实施状态（2026-09-01）

已完成。ExpenseOrchestrator 使用单个 Serializable 事务创建账本、用途拆分、保健记录、衣柜购买、消耗品采购和库存；RefundOrchestrator 锁定原支出并按用途校验剩余可退金额。网页和 API 均支持幂等键，家庭总览已接入真实月度净支出、模块对比、库存提醒与最近事件。A09 复用 `yudan-wupin` 既有结构，未执行生产数据库迁移。

## 17. A10：历史数据迁移 Agent

### 目标

从两个旧项目安全迁移数据。

### 允许修改

- `scripts/migration/**`
- `docs/migration/**`
- `tests/fixtures/migration/**`

不得修改 Prisma Migration。

### 任务

- 只读导出 Yudan-log；
- 只读导出 yudan-baby-pantry；
- 转换账本；
- 转换健康记录；
- InventoryBatch → StockEntry；
- 转换产品组和日志；
- 迁移成长日志；
- 保留 legacy ID；
- 生成迁移报告；
- 数量、金额、库存和关联校验；
- Dry Run；
- 可重复执行和幂等；
- 回退说明。

### 验收

- 收入、支出、月度汇总一致；
- 每个产品库存一致；
- 健康记录数量一致；
- 未识别历史账目进入 OTHER，不强猜业务关联；
- 迁移脚本不修改源数据库；
- Dry Run 不写目标数据库。

### 实施状态（2026-09-01）

已完成代码与运行手册。迁移工具通过只读事务同时盘点 `YUDAN` 和原位保留的 `yudan-wupin`，使用精确十进制生成逐月账本、模块、健康与逐产品库存报告；平台 Schema、非负库存、单一宝宝生日、备份确认和目标库名称均为强制 Gate。导入使用单个目标事务、咨询锁、legacy 映射和提交前对账，可重复执行；当前生产 `yudan-wupin` 尚未应用平台 Schema，因此生产导入仍被主动阻断。

## 18. A11：QA、安全与审查 Agent

### 目标

验证完整业务链路、权限边界和安全要求。

### 允许修改

- `tests/e2e/**`
- `tests/integration/**`
- `tests/security/**`
- `docs/test-reports/**`

QA 可提交修复建议；跨模块生产修复应由对应模块 Agent 完成。

### 任务

- 单元测试覆盖率检查；
- 邀请系统安全测试；
- RBAC 权限矩阵；
- IDOR / 跨家庭越权；
- API Key Scope；
- 组合记账原子性；
- Decimal 精度；
- 库存并发；
- R2 私有资源；
- 上传类型欺骗；
- OAuth Callback；
- CSRF / Origin；
- 幂等重试；
- 迁移结果；
- 移动端 E2E；
- 可访问性基础检查；
- Secret 扫描。

### 发布阻断问题

以下任一存在不得发布：

- 未授权读取健康或账本数据；
- 邀请码可重复使用；
- 普通用户可提权；
- 跨家庭访问；
- 账本重复计算；
- 库存出现负数；
- 组合事务半成功；
- 私有 R2 对象公开；
- Secret 进入客户端或 Git；
- 历史金额迁移不一致。

### 实施状态（2026-09-01）

代码层审查已完成：97 条单元/安全测试、6 条桌面与移动浏览器 E2E、依赖审计、Secret 扫描和 Cloudflare 私有桶检查通过；修复了跨来源写请求防护、认证标题语义和 Prisma 配置链漏洞。生产结论仍为 NO-GO：`yudan-wupin` 尚未应用平台 Schema，两个 Supabase 项目未启用泄露密码保护，真实数据库并发/RBAC/迁移目标对账未完成，Vercel 尚无 `yudan-platform` 项目。详见 `docs/test-reports/A11-release-readiness-2026-09-01.md`。

## 19. A12：发布与运维 Agent

### 目标

完成 Vercel、Cloudflare、Supabase 的安全发布和切换。

### 允许修改

- `vercel.json`
- `wrangler*.jsonc` 或 Cloudflare 配置
- `.github/workflows/**` 发布部分
- `docs/deployment/**`
- `docs/runbooks/**`

### 任务

- Vercel Git Integration；
- Preview / Production 环境变量；
- 数据库 Migration 独立步骤；
- Preview E2E 后 Promote；
- Vercel 回滚；
- R2 Bucket、CORS、Lifecycle；
- Cloudflare 自定义资源域名；
- Supabase OAuth Redirect URL；
- 域名切换；
- 旧 API 兼容监控；
- 日志和错误监控；
- 备份和恢复 Runbook；
- 发布检查表。

### 验收

- Preview 验证通过后才可生产 Promote；
- Migration 与应用发布顺序明确；
- 可以回滚应用；
- 数据库变更有兼容窗口；
- R2 私有 Bucket 不公开；
- OAuth 正式、Preview 和本地回调正确；
- 生产 Secret 不出现在构建日志。

### 实施状态（2026-09-01）

已完成 A12 发布底座：CI 增加 Prisma、依赖审计和桌面/移动端浏览器检查；数据库 Migration 与 Vercel Preview 验证/Promote 拆为两个受保护的人工工作流；新增无鉴权、无缓存的健康与就绪接口、Preview/Production 环境隔离校验、Vercel Deployment Protection 自动化支持，以及发布、回滚、备份恢复 Runbook。Cloudflare 两个生产 Bucket 的私有性、域名、CORS 和 7 天未完成分片上传清理规则已核对。

云端 Preview 尚未创建：当前 Supabase `yudan-wupin` 没有开发分支，创建 Branch 涉及费用确认；Vercel 尚无 `yudan-platform` 项目。完成费用确认并创建隔离数据库/R2 后，才进入 Preview Migration、真实数据库 Gate 和 Vercel Preview，生产仍保持 NO-GO。

## 20. Schema 变更请求模板

功能 Agent 需要新字段时，在 PR 或任务中提交：

```markdown
## Schema Change Request

- 请求 Agent：
- 所属任务：
- 模块：
- 业务原因：
- 新增 / 修改模型：
- 字段及类型：
- 是否可空：
- 默认值：
- 唯一约束：
- 索引：
- 外键与删除策略：
- 历史数据回填：
- 回滚方式：
- 对其他模块的影响：
```

A02 合并 Schema 和 Migration 后，功能 Agent 再继续依赖该字段的代码。

## 21. Agent 交接模板

每个 Agent 完成工作后必须提供：

```markdown
## Agent Handoff

- 任务编号：
- 分支：
- PR：
- 完成内容：
- 未完成内容：
- 修改目录：
- 新增 Migration：
- 新增环境变量：
- API 变化：
- 权限变化：
- 测试命令：
- 测试结果：
- 已知风险：
- 后续依赖：
- 回退方式：
```

## 22. 合并顺序

推荐合并顺序：

1. A00 架构契约；
2. A01 项目初始化；
3. A02 第一版 Schema；
4. A03、A04、A05；
5. A02 接收模块 Schema 补充；
6. A06、A07、A08；
7. A09；
8. A10；
9. A11 修复循环；
10. A12 发布配置；
11. 最终迁移与生产切换。

禁止为追求并行速度跳过基础契约和数据库 Gate。

## 23. 阶段 Gate

### Gate 0：可以开始基础开发

- 架构契约冻结；
- 目录和类型规范完成；
- CI 可运行。

### Gate 1：可以开始模块开发

- Schema 第一版可迁移；
- Auth Context 接口稳定；
- authorize 接口稳定；
- LedgerService 接口稳定；
- MediaStorage 接口稳定。

### Gate 2：可以开始跨模块集成

- 保健、衣柜、消耗品核心 Service 完成；
- 各模块权限测试通过；
- 各模块独立流程通过。

### Gate 3：可以迁移生产数据

- 迁移 Dry Run 通过；
- 源数据备份完成；
- 数量、金额和库存校验脚本完成；
- 目标环境权限验证完成。

### Gate 4：可以发布

- 阻断级安全问题为零；
- E2E 全部通过；
- Vercel Production Build 通过；
- R2 权限和 CORS 验证通过；
- OAuth 回调验证通过；
- 回滚演练完成。

## 24. 全局完成定义

平台完成需要同时满足：

- 一个账本汇总保健、衣柜、消耗品和其他开支；
- 一笔付款可以拆分多个模块；
- 账本不存在重复金额；
- 超级管理员、邀请码注册和权限系统可用；
- 跨家庭数据隔离有效；
- 健康数据默认私有；
- 消耗品库存与旧系统一致；
- 用户不需要理解批次；
- 图片和附件安全存放在 Cloudflare R2；
- Vercel Preview、Production 和回滚可用；
- 旧账本、健康、库存和日志迁移完成；
- lint、typecheck、unit、integration、E2E、build 全部通过；
- 文档、运行手册、备份和回退方案完整。

## 25. 推荐启动顺序

第一轮同时启动三个 Agent：

```text
A00 架构契约
A01 平台初始化准备
A11 测试框架准备
```

A00 完成后：

```text
A01 完成项目骨架
        ↓
A02 建立第一版数据库
        ↓
A03 认证权限 + A04 账本 + A05 资源 并行
```

第二轮：

```text
A06 保健 + A07 衣柜 + A08 消耗品 并行
```

第三轮：

```text
A09 组合记账
A10 最终迁移
A11 全面验证
A12 发布切换
```

按照 Gate 推进，不按 Agent 完成速度直接合并未验证代码。
