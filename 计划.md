# 鱼蛋家庭平台合并与建设计划

> 仓库：<https://github.com/Chenming00/yudan-platform>  
> 来源项目：
>
> - <https://github.com/Chenming00/yudan-baby-pantry>
> - <https://github.com/Chenming00/Yudan-log>
>
> 文档状态：架构与实施基线  
> 更新日期：2026-09-01

## 1. 项目背景

目前家庭相关功能分散在两个项目中：

- `yudan-baby-pantry`：负责婴儿消耗品库存，包括产品、入库、出库、库存预警、临期提醒、补货清单、操作日志、产品组、PWA 和外部 API。
- `Yudan-log`：负责家庭账本、宝宝体重与疫苗、成长日志，以及相关统计和 API。

新项目 `yudan-platform` 的目标不是简单拼接两个网站，而是建设一个“单项目、多业务模块”的家庭管理平台。所有模块共享登录、数据库、UI、部署和基础设施，同时保持清晰的业务边界，便于长期维护和后续扩展。

## 2. 总体目标

最终平台提供以下模块：

1. 家庭总览
2. 用品与库存
3. 家庭账本
4. 宝宝健康
5. 成长日志
6. 统一设置与权限
7. 统一 API 与自动化入口

目标形态：

```text
鱼蛋家庭平台
├── 总览
├── 用品
│   ├── 产品
│   ├── 采购入库
│   ├── 消耗出库
│   ├── 库存预警
│   └── 补货清单
├── 账本
│   ├── 收支记录
│   ├── 月度统计
│   └── 分类统计
├── 宝宝健康
│   ├── 体重 / 身高 / 头围
│   └── 疫苗计划与接种记录
├── 成长日志
└── 设置
```

## 3. 核心架构决策

### 3.1 架构类型

采用模块化单体架构：

- 一个代码仓库；
- 一个 Next.js 应用；
- 一个主要数据库；
- 一个认证体系；
- 一个部署单元；
- 多个相互隔离的业务模块。

当前阶段不采用微服务。家庭自用规模下，微服务会增加认证、部署、日志、数据库事务和运维成本，而不会带来足够收益。

### 3.2 主技术栈

以 `yudan-baby-pantry` 的技术栈作为主基础：

| 层面 | 技术 |
|---|---|
| 全栈框架 | Next.js App Router |
| 前端 | React + TypeScript |
| UI | Tailwind CSS + Radix UI |
| 数据访问 | Prisma |
| 数据库 | Supabase PostgreSQL |
| 认证 | Supabase Auth + GitHub OAuth |
| 部署 | Vercel |
| 内容 | Markdown + Frontmatter |
| 图表 | Recharts |
| 通知 | Sonner |
| PWA | Web App Manifest |

`Yudan-log` 中的 Astro 页面迁移为 Next.js 页面；可复用的 React 组件、Markdown 内容和业务计算逻辑保留。

### 3.3 模块依赖规则

业务访问路径统一为：

```text
页面 / API
    ↓
模块 Service
    ↓
模块 Repository
    ↓
Prisma
    ↓
Supabase PostgreSQL
```

约束：

- 页面和 Route Handler 不直接写 Prisma 查询；
- 模块不得直接读取其他模块的内部表；
- 跨模块操作通过公开的 Service 完成；
- 公共 UI、认证、日期、错误处理放入共享层；
- 金额统一使用 Decimal，禁止使用 Float 计算账务金额；
- 关键写操作必须在数据库事务内完成。

## 4. 推荐目录结构

```text
yudan-platform/
├── app/
│   ├── page.tsx
│   ├── inventory/
│   ├── ledger/
│   ├── health/
│   ├── blog/
│   ├── settings/
│   └── api/
│       ├── inventory/
│       ├── purchases/
│       ├── ledger/
│       ├── health/
│       └── blog/
├── modules/
│   ├── inventory/
│   │   ├── domain/
│   │   ├── services/
│   │   ├── repositories/
│   │   ├── schemas/
│   │   └── types.ts
│   ├── purchases/
│   ├── ledger/
│   ├── health/
│   └── blog/
├── components/
│   ├── layout/
│   ├── navigation/
│   └── ui/
├── lib/
│   ├── auth/
│   ├── db/
│   ├── api/
│   ├── date/
│   └── errors/
├── prisma/
│   ├── schema.prisma
│   ├── migrations/
│   └── seed.ts
├── content/
│   └── blog/
├── public/
├── tests/
└── package.json
```

## 5. 页面与路由规划

| 路径 | 模块 | 用途 |
|---|---|---|
| `/` | 总览 | 库存预警、最近体重、疫苗、本月支出、最近日志 |
| `/inventory` | 库存 | 产品与库存总览 |
| `/inventory/inbound` | 库存 | 采购入库或非采购入库 |
| `/inventory/consume` | 库存 | 消耗出库 |
| `/inventory/replenish` | 库存 | 补货清单 |
| `/inventory/products/[code]` | 库存 | 产品详情 |
| `/inventory/entries` | 库存 | 高级入库记录 |
| `/ledger` | 账本 | 收支列表与统计 |
| `/ledger/[id]` | 账本 | 收支详情及关联采购 |
| `/health` | 健康 | 宝宝健康总览 |
| `/health/weight` | 健康 | 生长记录与趋势 |
| `/health/vaccines` | 健康 | 疫苗计划与接种 |
| `/blog` | 日志 | 成长日志列表 |
| `/blog/[slug]` | 日志 | 文章详情 |
| `/settings` | 设置 | 账号、API Key、数据导出等 |

移动端主导航建议固定为：

```text
总览｜用品｜健康｜账本｜日志
```

管理、设置、入库记录等低频入口放入二级菜单。

## 6. 模块边界

### 6.1 库存模块

负责：

- 产品资料；
- 当前库存；
- 安全库存；
- 采购入库；
- 非采购入库；
- 消耗出库；
- 临期与过期提醒；
- 补货清单；
- 库存变动日志；
- 库存盘点和修正。

不负责：

- 账本统计；
- 支付方式；
- 收支分类；
- 家庭财务报表。

### 6.2 采购模块

采购模块是库存与账本的桥梁，负责：

- 一次采购的基本信息；
- 采购中的多个商品；
- 采购总金额与明细金额；
- 创建对应库存入库记录；
- 可选创建对应账本支出；
- 采购取消、冲销和关联查询。

### 6.3 账本模块

负责：

- 收入与支出；
- 分类；
- 交易时间；
- 支付方式；
- 月度、日度和分类统计；
- 手工账目；
- 采购产生的自动账目。

账本不直接修改库存。采购产生的账本记录应通过采购模块创建。

### 6.4 健康模块

负责：

- 宝宝基础资料；
- 体重、身高、头围；
- 生长趋势与参考标准；
- 疫苗目录；
- 建议接种日期；
- 实际接种记录。

健康数据与账本、库存保持独立，后续如需要关联疫苗支出，应通过采购或账本的通用关联机制完成。

### 6.5 成长日志模块

负责：

- Markdown 内容；
- Frontmatter；
- 日志列表；
- 标签和时间线；
- 文章详情。

第一阶段继续使用 Git 管理内容，不引入数据库 CMS。

## 7. 库存模型：弱化批次，保留入库记录

### 7.1 用户体验决策

普通用户不需要理解或手动管理“批次”。

日常页面只显示：

```text
爱他美奶粉
当前库存：4 罐
安全库存：2 罐
最近到期：2027-02-01
```

系统内部保留“入库记录”，用于处理：

- 不同到期日期；
- 不同采购价格；
- 采购与账本关联；
- 先进先出或先到期先出；
- 撤销和审计；
- 库存成本计算。

原 `InventoryBatch` 建议重命名为 `StockEntry` 或逐步迁移到同等语义的模型。UI 中统一称为“入库记录”，不展示批次编码作为主要概念。

### 7.2 推荐库存结构

```text
Product
├── 产品资料
├── 安全库存
└── StockEntry[]
       ├── 入库数量
       ├── 剩余数量
       ├── 到期日期（可选）
       ├── 单位成本（可选）
       └── PurchaseItem（可选）
```

当前库存：

```text
SUM(有效 StockEntry.remaining_quantity)
```

出库顺序：

1. 有库存；
2. 未停用；
3. 未过期；
4. 到期日期更早的优先；
5. 没有到期日时，入库更早的优先。

用户只选择产品和数量，系统自动分配入库记录。

### 7.3 入库来源

```text
PURCHASE       采购
GIFT           赠送
TRANSFER       转入
ADJUSTMENT     盘点调整
HISTORICAL     历史补录
```

只有 `PURCHASE` 默认生成账本支出，其余来源默认不记账。

## 8. 库存与记账关联方案

### 8.1 核心业务关系

```text
Purchase
├── 一个可选 Transaction
└── 多个 PurchaseItem
       ├── 一个 Product
       └── 一个或多个 StockEntry
```

示例：

```text
采购单 PO-20260901-001
├── 总金额：¥417
├── 账本支出：宝宝用品 ¥417
├── 奶粉 2 罐
└── 尿不湿 3 包
```

### 8.2 推荐模型

```prisma
model Purchase {
  id              String         @id @default(uuid())
  purchaseNo      String         @unique @map("purchase_no")
  purchasedAt     DateTime       @map("purchased_at")
  totalAmount     Decimal        @db.Decimal(12, 2) @map("total_amount")
  supplier        String?
  paymentMethod   String?        @map("payment_method")
  note            String?
  status          PurchaseStatus @default(COMPLETED)
  transactionId   String?        @unique @map("transaction_id")
  createdAt       DateTime       @default(now()) @map("created_at")
  updatedAt       DateTime       @updatedAt @map("updated_at")

  transaction Transaction?  @relation(fields: [transactionId], references: [id])
  items       PurchaseItem[]

  @@map("purchases")
}

model PurchaseItem {
  id           String   @id @default(uuid())
  purchaseId   String   @map("purchase_id")
  productCode  String   @map("product_code")
  quantity     Int
  unitCost     Decimal? @db.Decimal(12, 2) @map("unit_cost")
  lineAmount   Decimal? @db.Decimal(12, 2) @map("line_amount")
  note         String?

  purchase Purchase     @relation(fields: [purchaseId], references: [id])
  product  Product      @relation(fields: [productCode], references: [productCode])
  entries  StockEntry[]

  @@map("purchase_items")
}

model Transaction {
  id              String          @id @default(uuid())
  amount          Decimal         @db.Decimal(12, 2)
  type            TransactionType
  category        String?
  note            String?
  paymentMethod   String?         @map("payment_method")
  transactionTime DateTime        @map("transaction_time")
  sourceType      String?         @map("source_type")
  sourceId        String?         @map("source_id")
  createdAt       DateTime        @default(now()) @map("created_at")

  purchase Purchase?

  @@index([transactionTime])
  @@index([sourceType, sourceId])
  @@map("transactions")
}
```

最终 Prisma Schema 可以按实际 Prisma 版本和命名约定调整，但业务关系必须保持。

### 8.3 采购入库事务

采购入库需要在一个数据库事务中完成：

```text
创建采购单
    ↓
创建采购明细
    ↓
创建库存入库记录
    ↓
创建库存日志
    ↓
可选创建账本支出
    ↓
提交事务
```

任何一步失败，整笔操作回滚，避免出现“库存增加但账本没有支出”或“账本支出存在但库存未增加”。

### 8.4 出库与账本的关系

默认使用现金收支口径：

- 购买时记录支出；
- 入库时增加库存；
- 消耗时减少库存；
- 消耗时不再次记录支出。

否则会产生重复支出。

后续如需分析“实际消耗成本”，通过以下方式计算：

```text
每次出库数量 × 对应入库记录单位成本
```

消耗成本属于统计指标，不写入普通家庭支出。

### 8.5 入库页面

第一版采购入库表单：

```text
商品        数量    单价/金额    到期日（可选）
奶粉          2        240       2027-02-01
尿不湿        3        177       无

购买日期：2026-09-01
购买渠道：京东
支付方式：信用卡
账本分类：宝宝用品
总金额：417
[√] 同步生成账本支出
```

非采购入库表单：

```text
商品：奶粉
数量：2
来源：赠送
到期日：可选
备注：可选
```

## 9. 数据库规划

最终使用一个 Supabase PostgreSQL 项目，通过 Prisma 管理业务表和迁移。

表按模块分组：

### 库存与采购

- `products`
- `stock_entries`
- `inventory_logs`
- `purchases`
- `purchase_items`
- `product_groups`
- `product_group_items`

### 账本

- `transactions`

### 宝宝健康

- `baby_profiles`
- `weight_records`
- `vaccine_catalog`
- `vaccine_records`

### 认证与系统

Supabase Auth 继续负责用户身份。业务数据根据需要保留 `user_id` 或家庭空间 ID。为了将来支持多位家庭成员，建议预留：

- `households`
- `household_members`

第一阶段可以只有一个家庭空间，但新表应避免继续把特定邮箱作为永久业务主键。

## 10. 权限与安全

### 10.1 访问级别

```text
访客
├── 可查看允许公开的页面
└── 不可修改数据

已登录成员
├── 查看家庭数据
└── 根据角色修改数据

API 客户端
└── 通过 API Key 调用授权接口
```

建议角色：

- `OWNER`
- `EDITOR`
- `VIEWER`

### 10.2 安全原则

- Prisma 和数据库密钥只在服务端使用；
- API Key 不使用 `NEXT_PUBLIC_` 前缀；
- 写操作校验登录身份或 API Key；
- Cookie 写请求校验 Origin；
- API Key 使用常量时间比较；
- 日志不得记录完整 API Key、Cookie 或数据库连接串；
- 数据迁移前必须备份；
- 生产环境禁止使用 `prisma db push` 替代正式 Migration；
- 健康数据默认不公开。

## 11. API 规划

统一使用模块化路径：

### 库存与采购

- `GET /api/inventory/products`
- `GET /api/inventory/products/[code]`
- `POST /api/purchases`
- `GET /api/purchases/[id]`
- `POST /api/inventory/consume`
- `POST /api/inventory/adjust`
- `GET /api/inventory/replenish`

### 账本

- `GET /api/ledger/transactions`
- `POST /api/ledger/transactions`
- `PATCH /api/ledger/transactions/[id]`
- `DELETE /api/ledger/transactions/[id]`
- `GET /api/ledger/summary`

### 宝宝健康

- `GET /api/health/profile`
- `GET /api/health/weight`
- `POST /api/health/weight`
- `GET /api/health/vaccines`
- `POST /api/health/vaccines`

迁移期间保留旧 API 兼容层，特别是 Telegram、Agent Skill 和其他脚本正在使用的接口。兼容层稳定运行并完成调用方迁移后再移除。

所有写接口应支持幂等键，避免移动网络重试或 Agent 重试造成重复采购、重复入库、重复记账。

## 12. 数据迁移计划

### 12.1 原则

- 先复制，后切换；
- 先校验，后删除；
- 原项目和原数据库在新平台稳定前保持只读或可回退；
- 不直接在生产数据上做不可逆转换；
- 每张表记录迁移前后数量和金额校验结果。

### 12.2 库存数据迁移

映射：

```text
Product                    → Product
InventoryBatch             → StockEntry
InventoryLog               → InventoryLog
ProductGroup               → ProductGroup
ProductGroupItem           → ProductGroupItem
```

原有批次编码可以保存在 `legacy_batch_code`，用于审计和回退，但新 UI 不突出批次概念。

校验：

- 产品数量一致；
- 每个产品当前库存一致；
- 入库数量与剩余数量合法；
- 临期和过期统计一致；
- 库存日志数量一致；
- 出库后库存不能为负数。

### 12.3 账本数据迁移

原 `transactions` 迁移到统一 Prisma 模型。

校验：

- 交易总数一致；
- 收入总额一致；
- 支出总额一致；
- 每月汇总一致；
- 时间按 Asia/Shanghai 展示一致；
- 历史手工账目不强制关联采购。

### 12.4 健康数据迁移

映射：

```text
yudan_dashboards       → baby_profiles
yudan_weight_records   → weight_records
yudan_vaccine_catalog  → vaccine_catalog
yudan_vaccine_records  → vaccine_records
```

校验：

- 宝宝生日一致；
- 体重记录日期和数量一致；
- 同日唯一约束一致；
- 疫苗计划和实际接种记录数量一致；
- 接种记录与疫苗目录的关联全部有效。

### 12.5 成长日志迁移

- 保留文章 slug；
- 保留日期、标题、描述和标签；
- 保留 Markdown 内容；
- 保留代码高亮和 GFM；
- 检查所有内部链接和静态资源；
- 旧 URL 建立跳转或保持路径不变。

## 13. 分阶段实施计划

### 阶段 0：平台初始化

目标：建立可构建、可测试、可部署的空平台。

任务：

- 初始化 Next.js + TypeScript；
- 配置 Tailwind、Radix UI、ESLint；
- 配置 Prisma；
- 建立模块目录；
- 建立基础布局和移动导航；
- 配置环境变量示例；
- 配置基础 CI；
- 建立架构决策记录。

完成标准：

- `npm run lint` 通过；
- `npx tsc --noEmit` 通过；
- `npm run build` 通过；
- Vercel Preview 可访问。

### 阶段 1：迁移库存模块

目标：首先保持 Pantry 核心能力不回退。

任务：

- 迁移产品、入库、出库、补货和日志；
- 将批次 UI 改为入库记录；
- 保留自动出库顺序；
- 保留库存调整、撤销和数据导出；
- 迁移 PWA 和公开产品组；
- 建立库存回归测试。

完成标准：

- 产品和库存统计一致；
- 入库、出库、撤销正常；
- 临期和补货提醒正确；
- 旧 Pantry 核心流程可在新平台完成。

### 阶段 2：迁移账本模块

目标：将原账本完整迁入新平台。

任务：

- 增加 Transaction Prisma 模型；
- 迁移账本页面、查询和统计；
- 迁移原 API 兼容层；
- 验证 Telegram 和 API Key；
- 添加交易详情页。

完成标准：

- 历史记录和月度统计一致；
- 增删改正常；
- 外部记账入口正常；
- 金额全程使用 Decimal。

### 阶段 3：建立采购关联

目标：一次操作同时完成采购、库存和记账。

任务：

- 新增 Purchase 和 PurchaseItem；
- 新增采购入库页面；
- 实现数据库事务；
- 实现非采购入库；
- 实现账本与采购双向跳转；
- 实现取消和冲销规则；
- 增加幂等保护。

完成标准：

- 采购可包含多个商品；
- 采购入库和支出原子完成；
- 非采购入库不自动记账；
- 出库不重复产生支出；
- 删除或取消操作不会留下孤立数据。

### 阶段 4：迁移健康模块

目标：迁移宝宝资料、生长记录和疫苗。

任务：

- 建立健康模块模型；
- 迁移历史数据；
- 迁移图表和标准计算；
- 迁移疫苗目录和建议日期；
- 配置健康数据权限。

完成标准：

- 历史记录数量一致；
- 图表与计算结果一致；
- 未授权用户无法读取真实健康数据。

### 阶段 5：迁移成长日志

目标：保留现有文章和 URL。

任务：

- 迁移 Markdown；
- 实现文章列表与详情；
- 迁移 GFM、代码高亮；
- 修复资源和链接；
- 增加总览页最近文章。

完成标准：

- 所有文章可访问；
- slug 和发布日期正确；
- Markdown 渲染一致。

### 阶段 6：统一总览与正式切换

任务：

- 建立家庭总览；
- 统一导航与视觉样式；
- 完成旧域名跳转；
- 更新 API 文档；
- 完成生产数据最终迁移；
- 观察运行一至两周；
- 原项目进入只读或归档状态。

## 14. Git 与协作策略

建议分支：

```text
main
├── feature/platform-bootstrap
├── feature/inventory-module
├── feature/ledger-module
├── feature/purchase-link
├── feature/health-module
└── feature/blog-module
```

规则：

- `main` 始终保持可构建；
- 每个阶段使用独立 PR；
- 数据库 Migration 与使用它的代码放在同一 PR；
- PR 必须包含迁移说明、回退方式和测试结果；
- 不直接复制两个项目的全部文件后一次性提交；
- 不在同一个 PR 同时迁移所有模块。

## 15. 测试计划

### 单元测试

- 金额计算；
- 采购总额和明细合计；
- 库存聚合；
- 出库顺序；
- 补货数量；
- 日期与临期计算；
- 疫苗建议日期；
- 权限判断。

### 集成测试

- 采购创建后同时生成入库记录和支出；
- 任一步失败时事务回滚；
- 非采购入库不生成支出；
- 出库不生成支出；
- 重复幂等键不会重复写入；
- 采购取消时库存和账本按规则冲销；
- 历史交易统计正确。

### 端到端测试

- GitHub 登录；
- 手机端采购入库；
- 快速出库；
- 账本新增与编辑；
- 查看宝宝健康记录；
- 阅读成长日志；
- API Key 调用；
- PWA 安装与离线状态提示。

## 16. 验收标准

平台发布前必须满足：

- 一个域名可以访问所有模块；
- 一个账号体系管理所有权限；
- 库存数据迁移前后总量一致；
- 账本收入、支出和月度汇总一致；
- 健康记录数量和日期一致；
- 成长日志 URL 可用；
- 采购入库与账本支出在同一事务中完成；
- 普通用户无需理解批次；
- 系统仍能正确处理到期日和自动出库；
- 出库不会重复记账；
- API Key、Cookie、数据库密钥不会泄露；
- lint、类型检查、测试和生产构建全部通过；
- 数据迁移有备份和回退方案。

## 17. 风险与应对

| 风险 | 应对 |
|---|---|
| 两个 Supabase 项目数据合并出错 | 全量备份、只读迁移、逐表校验 |
| Astro 到 Next.js 页面行为变化 | 分模块迁移并做截图与功能回归 |
| 批次改名导致库存逻辑回退 | 数据层保留入库记录，先改 UI 再迁模型 |
| 采购自动记账造成重复支出 | 幂等键、唯一关联、事务和回归测试 |
| 外部 API 路径变化 | 保留兼容路由并记录弃用周期 |
| 金额精度问题 | 全部改为 Decimal |
| 健康数据意外公开 | 默认私有、服务端校验、权限测试 |
| 一次性合并范围过大 | 按阶段 PR，库存优先，逐模块验收 |

## 18. 第一版范围

第一版必须完成：

- 统一登录；
- 用品库存；
- 采购入库；
- 入库自动记账；
- 非采购入库；
- 消耗出库；
- 家庭账本；
- 体重和疫苗；
- 成长日志；
- 家庭总览；
- 数据迁移与旧 API 兼容。

第一版暂不包含：

- 多家庭空间完整 UI；
- 复杂预算；
- 复式记账；
- 多币种；
- 发票 OCR；
- 电商订单自动同步；
- 微服务；
- 消息队列；
- 独立移动 App。

## 19. 后续扩展方向

平台稳定后可考虑：

- 多位家庭成员与角色；
- 家庭预算；
- 商品价格趋势；
- 每月消耗成本；
- 疫苗或医疗支出关联；
- 条形码扫码；
- 购物清单；
- Telegram / Agent 自然语言操作；
- 数据备份与恢复；
- 家庭事件时间线；
- 通知和定时提醒。

## 20. 最终结论

`yudan-platform` 应建设为一个 Next.js 模块化单体：

```text
一个项目
├── 多个业务模块
├── 一套认证
├── 一个主要数据库
├── 一套 UI
├── 一组统一 API
└── 一个部署单元
```

库存与记账通过“采购”关联；用户不需要操作批次，但系统内部保留入库记录，从而同时获得简单体验、到期管理、成本追踪、撤销能力和数据一致性。

实施顺序固定为：

```text
平台初始化
→ 库存迁移
→ 账本迁移
→ 采购关联
→ 健康迁移
→ 日志迁移
→ 统一总览
→ 数据切换
```

任何阶段都应保持可构建、可测试和可回退。
