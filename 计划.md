# 鱼蛋家庭开支平台建设计划

> 仓库：<https://github.com/Chenming00/yudan-platform>  
> 来源项目：
>
> - <https://github.com/Chenming00/yudan-baby-pantry>
> - <https://github.com/Chenming00/Yudan-log>
>
> 文档状态：架构与实施基线  
> 更新日期：2026-09-01

## 1. 产品定位

`yudan-platform` 是一个以“家庭开支账本”为核心的单项目、多模块家庭管理平台。

账本是整个平台的财务主线，所有真实发生的收入、支出、退款都统一进入账本。儿童保健、衣柜、消耗品不是与账本平级的孤立系统，而是产生或解释账本开支的业务模块。

```text
家庭开支账本
├── 儿童保健
│   ├── 儿保检查
│   ├── 疫苗
│   ├── 医疗 / 药品
│   └── 生长记录
├── 衣柜
│   ├── 衣物购买
│   ├── 尺码与季节
│   ├── 在穿 / 待穿 / 闲置
│   └── 单件成本
├── 消耗品
│   ├── 奶粉、尿不湿、湿巾等
│   ├── 采购入库
│   ├── 使用消耗
│   ├── 库存与补货
│   └── 到期提醒
└── 其他记账
    ├── 餐饮
    ├── 交通
    ├── 居家
    ├── 收入
    └── 自定义分类
```

核心原则：

> 钱的事实只在账本中保存一次；业务模块保存“这笔钱为什么发生、买了什么、产生了什么记录”。

## 2. 建设目标

平台最终实现：

1. 一个统一账本，汇总全部家庭收支；
2. 儿童保健开支与儿保、疫苗、医疗记录关联；
3. 衣柜开支与衣物、尺码、季节和使用状态关联；
4. 消耗品开支与采购、库存和补货关联；
5. 其他开支可以直接记账，不要求业务记录；
6. 一笔付款可以拆分到多个模块；
7. 一个模块中的业务记录可以追溯到账本；
8. 所有模块共享登录、数据库、UI、API 和部署；
9. 保留旧项目数据，并提供可校验、可回退的迁移流程。

## 3. 总体架构

采用 Next.js 模块化单体：

- 一个 Git 仓库；
- 一个 Next.js 应用；
- 一个 Supabase PostgreSQL 数据库；
- 一套 Supabase Auth；
- 一套 Prisma 数据模型与 Migration；
- 一个 Vercel 部署单元；
- 多个边界清晰的业务模块。

当前阶段不采用微服务。家庭自用规模下，模块化单体可以保证跨模块数据库事务，同时避免额外的服务通信和运维成本。

技术栈：

| 层面 | 技术 |
|---|---|
| 全栈框架 | Next.js App Router |
| 前端 | React + TypeScript |
| UI | Tailwind CSS + Radix UI |
| 数据访问 | Prisma |
| 数据库 | Supabase PostgreSQL |
| 认证 | Supabase Auth + GitHub OAuth |
| 部署 | Vercel |
| 图表 | Recharts |
| 内容 | Markdown + Frontmatter |
| PWA | Web App Manifest |

业务访问路径：

```text
页面 / API
    ↓
应用编排 Service
    ↓
账本 Service + 业务模块 Service
    ↓
各模块 Repository
    ↓
Prisma Transaction
    ↓
Supabase PostgreSQL
```

约束：

- 页面和 Route Handler 不直接调用 Prisma；
- 金额统一使用 Decimal；
- 账本金额不能在多个业务表中重复作为财务事实；
- 跨模块写入必须通过应用编排 Service；
- 涉及账本和业务记录的操作必须使用数据库事务；
- 模块不能直接修改其他模块的内部数据；
- 退款、撤销优先使用反向记录和状态变化，不直接删除历史。

## 4. 信息架构

桌面与移动端的主要入口：

```text
总览｜账本｜保健｜衣柜｜消耗品
```

次要入口：

- 成长日志；
- 数据导出；
- 分类管理；
- 支付账户；
- API 与自动化；
- 用户与权限；
- 系统设置。

页面规划：

| 路径 | 用途 |
|---|---|
| `/` | 家庭开支总览 |
| `/ledger` | 全部收入和支出 |
| `/ledger/new` | 快速记账 / 组合记账 |
| `/ledger/[id]` | 账目详情与业务关联 |
| `/care` | 儿童保健时间线和开支 |
| `/care/checkups` | 儿保检查 |
| `/care/vaccines` | 疫苗计划与接种 |
| `/care/growth` | 体重、身高、头围 |
| `/wardrobe` | 衣柜总览 |
| `/wardrobe/items` | 衣物列表 |
| `/wardrobe/new` | 购衣 / 录入衣物 |
| `/consumables` | 消耗品库存总览 |
| `/consumables/purchase` | 消耗品采购入库 |
| `/consumables/consume` | 消耗出库 |
| `/consumables/replenish` | 补货清单 |
| `/blog` | 成长日志 |
| `/settings` | 平台设置 |

总览首页重点展示：

- 本月总支出；
- 儿童保健支出；
- 衣柜支出；
- 消耗品支出；
- 其他支出；
- 最近账目；
- 待接种疫苗；
- 衣物尺码提醒；
- 低库存与临期提醒。

## 5. 推荐目录结构

```text
yudan-platform/
├── app/
│   ├── page.tsx
│   ├── ledger/
│   ├── care/
│   ├── wardrobe/
│   ├── consumables/
│   ├── blog/
│   ├── settings/
│   └── api/
│       ├── ledger/
│       ├── care/
│       ├── wardrobe/
│       ├── consumables/
│       └── automation/
├── modules/
│   ├── ledger/
│   │   ├── domain/
│   │   ├── services/
│   │   ├── repositories/
│   │   ├── schemas/
│   │   └── types.ts
│   ├── care/
│   ├── wardrobe/
│   ├── consumables/
│   ├── journal/
│   └── dashboard/
├── application/
│   ├── expense-orchestrator.ts
│   ├── refund-orchestrator.ts
│   └── import-orchestrator.ts
├── components/
│   ├── layout/
│   ├── navigation/
│   ├── charts/
│   └── ui/
├── lib/
│   ├── auth/
│   ├── db/
│   ├── api/
│   ├── date/
│   ├── money/
│   └── errors/
├── prisma/
│   ├── schema.prisma
│   ├── migrations/
│   └── seed.ts
├── content/
│   └── blog/
├── tests/
└── package.json
```

## 6. 账本核心模型

### 6.1 Transaction：钱的唯一事实

每次收入、支出或退款保存为一条 Transaction：

```prisma
model Transaction {
  id              String          @id @default(uuid())
  type            TransactionType
  amount          Decimal         @db.Decimal(12, 2)
  occurredAt      DateTime        @map("occurred_at")
  payee           String?
  paymentMethod   String?         @map("payment_method")
  account         String?
  note            String?
  status          TransactionStatus @default(POSTED)
  createdAt       DateTime        @default(now()) @map("created_at")
  updatedAt       DateTime        @updatedAt @map("updated_at")

  allocations     TransactionAllocation[]

  @@index([occurredAt])
  @@map("transactions")
}
```

建议类型：

```text
EXPENSE   支出
INCOME    收入
REFUND    退款
```

### 6.2 TransactionAllocation：账目拆分

一笔付款可能包含多个场景。例如一张电商订单同时购买尿不湿、衣服和药品，因此不能只在 Transaction 上保存一个分类。

```prisma
model TransactionAllocation {
  id            String       @id @default(uuid())
  transactionId String       @map("transaction_id")
  module        ExpenseModule
  category      String
  amount        Decimal      @db.Decimal(12, 2)
  sourceType    String?      @map("source_type")
  sourceId      String?      @map("source_id")
  note          String?
  createdAt     DateTime     @default(now()) @map("created_at")

  transaction Transaction @relation(fields: [transactionId], references: [id])

  @@index([transactionId])
  @@index([module, category])
  @@index([sourceType, sourceId])
  @@map("transaction_allocations")
}
```

建议模块枚举：

```text
CHILD_CARE    儿童保健
WARDROBE      衣柜
CONSUMABLES   消耗品
OTHER         其他
```

约束：

```text
Transaction.amount
=
SUM(TransactionAllocation.amount)
```

所有模块统计都基于 Allocation 聚合，家庭总账基于 Transaction 聚合。

### 6.3 为什么需要拆分

示例：

```text
京东付款 ¥520
├── 消耗品：尿不湿 ¥300
├── 衣柜：连体衣 ¥150
└── 儿童保健：维生素 ¥70
```

账本中只有一笔 ¥520 的付款，同时可以正确统计三个模块，且不会重复计算总支出。

## 7. 儿童保健模块

儿童保健模块包含两类数据：

1. 健康事实：体重、身高、头围、疫苗、儿保和就诊记录；
2. 财务关联：挂号、检查、疫苗、药品和保健品支出。

推荐结构：

```text
CareRecord
├── CHECKUP       儿保
├── VACCINE       疫苗
├── MEDICAL       就诊
├── MEDICINE      药品
└── SUPPLEMENT    保健品
```

保健记录可以没有支出，例如免费疫苗；也可以关联一个账本 Allocation，例如自费疫苗。

核心关系：

```text
Transaction
└── TransactionAllocation(module = CHILD_CARE)
       └── CareRecord
```

典型流程：

```text
记录自费疫苗
├── 接种日期
├── 疫苗计划
├── 医院
├── 金额
└── 自动生成儿童保健支出
```

免费疫苗：

```text
只创建接种记录，不创建支出
```

第一版儿童保健分类：

- 儿保；
- 疫苗；
- 挂号；
- 检查；
- 药品；
- 保健品；
- 其他医疗。

原 `Yudan-log` 的体重和疫苗数据迁移到此模块。

## 8. 衣柜模块

衣柜模块负责记录儿童衣物及其成本，不承担账本总额计算。

建议记录：

- 名称；
- 品类；
- 品牌；
- 尺码；
- 适用季节；
- 购买日期；
- 购入价格；
- 数量；
- 状态；
- 图片；
- 备注。

衣物状态：

```text
PLANNED     计划购买
UNWORN      待穿
ACTIVE      在穿
OUTGROWN    已小
STORED      收纳
DONATED     已赠送
DISCARDED   已丢弃
SOLD        已出售
```

核心关系：

```text
Transaction
└── TransactionAllocation(module = WARDROBE)
       └── WardrobePurchase
            └── WardrobeItem[]
```

典型流程：

```text
购买衣服
├── 创建一笔账本支出
├── 分类到衣柜
├── 创建一条购买记录
└── 批量生成衣物
```

赠送衣物只创建 WardrobeItem，不生成支出。二手出售应创建 INCOME 或 REFUND 类型账目，并与衣物关联。

第一版衣柜分类：

- 上装；
- 下装；
- 连体衣；
- 外套；
- 内衣；
- 鞋袜；
- 帽子；
- 睡袋；
- 配饰；
- 其他。

衣柜统计：

- 本月 / 本年购衣支出；
- 各尺码支出；
- 各品类数量；
- 在穿、待穿、已小数量；
- 单次穿着成本作为后续能力，不进入第一版。

## 9. 消耗品模块

消耗品模块从 `yudan-baby-pantry` 迁移，负责：

- 产品资料；
- 当前库存；
- 安全库存；
- 采购入库；
- 赠送和历史补录；
- 消耗出库；
- 临期和过期提醒；
- 补货清单；
- 库存日志。

第一版分类：

- 奶粉与辅食；
- 尿不湿；
- 湿巾与纸品；
- 洗护；
- 清洁；
- 喂养用品；
- 药品与护理耗材；
- 其他消耗品。

### 9.1 弱化批次

普通用户不操作“批次”，只看到产品当前库存。

系统内部保留 `StockEntry` 入库记录，用于：

- 不同到期日；
- 不同采购价格；
- 自动选择先到期库存；
- 撤销和审计；
- 采购与账本关联。

```text
Product
└── StockEntry[]
    ├── 入库数量
    ├── 剩余数量
    ├── 到期日期（可选）
    ├── 单位成本（可选）
    └── ConsumablePurchaseItem
```

UI 使用“入库记录”，不把批次编码作为主要信息。

### 9.2 与账本关联

```text
Transaction
└── TransactionAllocation(module = CONSUMABLES)
       └── ConsumablePurchase
            └── ConsumablePurchaseItem[]
                 └── StockEntry[]
```

采购时：

- 创建账本支出；
- 创建消耗品采购记录；
- 创建入库记录；
- 创建库存日志。

出库时：

- 只减少库存；
- 不再次产生账本支出；
- 消耗成本只作为统计，不计入第二次支出。

非采购入库来源：

```text
GIFT          赠送
TRANSFER      转入
ADJUSTMENT    盘点调整
HISTORICAL    历史补录
```

这些来源默认不产生账本支出。

## 10. 其他记账

其他记账是通用入口，不要求创建儿童保健、衣柜或消耗品记录。

示例分类：

- 餐饮；
- 交通；
- 居家；
- 娱乐；
- 教育；
- 人情；
- 工资与收入；
- 退款；
- 自定义分类。

流程：

```text
快速记账
├── 金额
├── 收入 / 支出 / 退款
├── 日期
├── 分类
├── 收款方 / 商户
├── 支付方式
└── 备注
```

创建：

- 一条 Transaction；
- 一条 module = OTHER 的 TransactionAllocation。

## 11. 组合记账与跨模块事务

平台必须支持一次付款包含多个模块。

组合记账页面：

```text
付款总额：¥520
商户：京东
日期：2026-09-01

明细：
1. 尿不湿      消耗品    ¥300
2. 连体衣      衣柜      ¥150
3. 维生素      儿童保健  ¥70
```

保存时在一个 Prisma 数据库事务中：

1. 创建 Transaction；
2. 创建三条 TransactionAllocation；
3. 创建消耗品采购和 StockEntry；
4. 创建衣柜购买记录和 WardrobeItem；
5. 创建保健品记录；
6. 创建必要的业务日志；
7. 校验明细金额总和；
8. 提交事务。

任一步失败，整笔操作回滚。

## 12. 修改、退款、撤销和删除

### 12.1 修改

未关联业务记录的普通账目可以直接修改。

已关联业务模块的账目必须通过对应业务流程修改，防止只改金额、不改采购或物品记录。

### 12.2 退款

退款创建新的 REFUND Transaction，不直接把原支出金额改小。退款 Allocation 关联原 Transaction 或原业务记录。

例如退掉一件衣服：

- 创建退款账目；
- 修改衣物状态；
- 保留原购买记录；
- 记录退款关联。

### 12.3 撤销

刚创建且没有后续操作的组合记账可以整体撤销。涉及已消耗库存、已出售衣物或后续健康记录时，不允许直接删除，必须使用冲销流程。

### 12.4 删除原则

财务记录和库存日志默认软删除或状态化，不做无痕物理删除。物理删除仅用于未正式提交的草稿和明确的测试数据。

## 13. 数据库表规划

### 账本核心

- `transactions`
- `transaction_allocations`
- `categories`
- `payment_accounts`

### 儿童保健

- `baby_profiles`
- `growth_records`
- `vaccine_catalog`
- `vaccine_records`
- `care_records`

### 衣柜

- `wardrobe_purchases`
- `wardrobe_items`
- `wardrobe_categories`

### 消耗品

- `products`
- `stock_entries`
- `inventory_logs`
- `consumable_purchases`
- `consumable_purchase_items`
- `product_groups`
- `product_group_items`

### 系统

- `households`
- `household_members`
- `idempotency_keys`
- `audit_logs`

第一阶段可以只有一个家庭空间，但所有主要业务表应预留 `household_id`，避免以后重做数据隔离。

## 14. 权限与安全

角色：

```text
OWNER     所有者
EDITOR    可编辑家庭数据
VIEWER    只读
```

规则：

- 健康记录默认私有；
- 账本和衣柜默认只对家庭成员开放；
- 消耗品公开页面只显示经过明确授权的只读信息；
- Prisma 和数据库密钥只在服务端使用；
- API Key 不使用 `NEXT_PUBLIC_`；
- Cookie 写请求检查 Origin；
- API Key 使用常量时间比较；
- 日志禁止记录完整密钥和 Cookie；
- 写接口支持幂等键；
- 数据迁移前必须备份；
- 生产数据库只使用正式 Prisma Migration。

## 15. API 规划

### 账本

- `GET /api/ledger/transactions`
- `POST /api/ledger/transactions`
- `POST /api/ledger/composite`
- `PATCH /api/ledger/transactions/[id]`
- `POST /api/ledger/transactions/[id]/refund`
- `GET /api/ledger/summary`

### 儿童保健

- `GET /api/care/records`
- `POST /api/care/records`
- `GET /api/care/growth`
- `POST /api/care/growth`
- `GET /api/care/vaccines`
- `POST /api/care/vaccines`

### 衣柜

- `GET /api/wardrobe/items`
- `POST /api/wardrobe/purchases`
- `PATCH /api/wardrobe/items/[id]`
- `POST /api/wardrobe/items/[id]/sell`

### 消耗品

- `GET /api/consumables/products`
- `POST /api/consumables/purchases`
- `POST /api/consumables/consume`
- `POST /api/consumables/adjust`
- `GET /api/consumables/replenish`

旧 API 在迁移期保留兼容层，特别是 Telegram、Agent Skill 和现有脚本正在使用的接口。

## 16. 数据迁移计划

### 16.1 账本

原 `Yudan-log.transactions` 迁移为：

- 一条 Transaction；
- 一条 TransactionAllocation；
- 可识别的宝宝用品类别映射到对应模块；
- 无法可靠识别的历史记录统一映射为 OTHER；
- 不根据备注强行猜测具体业务对象。

校验：

- 交易数量一致；
- 收入总额一致；
- 支出总额一致；
- 退款总额一致；
- 每月统计一致；
- 时区显示一致。

### 16.2 儿童保健

```text
yudan_dashboards       → baby_profiles
yudan_weight_records   → growth_records
yudan_vaccine_catalog  → vaccine_catalog
yudan_vaccine_records  → vaccine_records
```

历史健康记录不强制生成支出。只有能够明确匹配的历史账目才建立关联。

### 16.3 消耗品

```text
Product                    → Product
InventoryBatch             → StockEntry
InventoryLog               → InventoryLog
ProductGroup               → ProductGroup
ProductGroupItem           → ProductGroupItem
```

原批次编码保存在 `legacy_batch_code`，新 UI 不突出批次。

历史库存采购价格可以形成迁移后的消耗品采购信息，但不能凭价格字段自动生成账本支出，避免与现有历史账目重复。

### 16.4 衣柜

现有两个来源项目没有完整衣柜数据模型，因此衣柜作为新模块建设。若现有账本中包含购衣记录，可以分类迁移，但不自动生成具体 WardrobeItem，除非用户后续补录。

### 16.5 成长日志

保留 Markdown、Frontmatter、slug、日期、标签、静态资源和旧 URL。

## 17. 分阶段实施计划

### 阶段 0：平台初始化

- 初始化 Next.js、TypeScript、Tailwind、Radix UI；
- 配置 Prisma、Supabase Auth 和 Vercel；
- 建立模块目录、基础布局和移动导航；
- 配置 lint、类型检查、测试和 CI；
- 建立环境变量示例。

完成标准：

- lint、类型检查和生产构建通过；
- Vercel Preview 可访问；
- 登录流程可用。

### 阶段 1：账本核心

- 建立 Transaction 和 TransactionAllocation；
- 迁移账本列表、增删改和统计；
- 建立分类与支付方式；
- 建立快速记账；
- 保留旧账本 API 兼容层；
- 迁移历史账目并完成金额校验。

完成标准：

- 总账和月度统计与旧系统一致；
- 支持 CHILD_CARE、WARDROBE、CONSUMABLES、OTHER；
- 支持一笔账拆分多个模块；
- 金额全部使用 Decimal。

### 阶段 2：儿童保健

- 迁移宝宝资料、体重和疫苗；
- 建立儿保、就诊、药品和保健品记录；
- 实现免费记录和付费记录；
- 实现保健记录与账本双向跳转；
- 配置健康数据权限。

完成标准：

- 历史健康数据一致；
- 自费记录可以同步记账；
- 免费疫苗不会生成支出；
- 未授权用户无法读取真实数据。

### 阶段 3：衣柜

- 建立衣物、购买记录、尺码、季节和状态；
- 实现购衣自动记账；
- 支持赠送录入；
- 支持闲置、赠送、丢弃、出售和退款；
- 建立衣柜统计。

完成标准：

- 购买衣物可同时进入账本和衣柜；
- 赠送衣物不产生支出；
- 出售和退款不会改写历史购买事实。

### 阶段 4：消耗品

- 迁移 Pantry 的产品、库存和日志；
- 将批次 UI 改为入库记录；
- 保留先到期先出、补货和临期提醒；
- 实现消耗品采购自动记账；
- 实现非采购入库；
- 迁移 PWA、产品组和外部 API。

完成标准：

- 迁移前后库存一致；
- 用户无需手动选择批次；
- 采购同时产生支出和库存；
- 消耗只减库存、不重复记账。

### 阶段 5：组合记账与总览

- 建立一次付款多模块明细；
- 实现跨模块数据库事务；
- 建立家庭总览和模块对比；
- 增加退款与冲销；
- 增加幂等保护和审计日志。

完成标准：

- 一笔付款可同时创建保健、衣柜和消耗品记录；
- 明细合计必须等于付款总额；
- 任一步失败整笔回滚；
- 总览不存在重复支出。

### 阶段 6：成长日志与正式切换

- 迁移成长日志；
- 完成旧域名和旧 API 跳转；
- 完成生产数据最终迁移；
- 更新文档；
- 观察运行一至两周；
- 原项目转为只读或归档。

## 18. 测试重点

单元测试：

- Decimal 金额计算；
- Allocation 合计；
- 模块分类统计；
- 库存聚合与出库顺序；
- 退款金额；
- 疫苗建议日期；
- 衣物状态变化；
- 权限判断。

集成测试：

- 普通记账只创建账本数据；
- 自费疫苗同时创建保健记录和支出；
- 免费疫苗不创建支出；
- 购衣同时创建账本、购买记录和衣物；
- 消耗品采购同时创建账本和库存；
- 消耗出库不创建第二笔支出；
- 组合记账任一步失败时全部回滚；
- 幂等重试不会重复记账或重复入库；
- 退款保留原始交易并正确冲减统计。

端到端测试：

- GitHub 登录；
- 手机端快速记账；
- 组合记账；
- 儿保和疫苗录入；
- 购衣和衣物状态管理；
- 消耗品采购、出库和补货；
- 月度统计；
- API Key 调用；
- PWA 安装与离线状态提示。

## 19. 验收标准

发布前必须满足：

- 账本是全平台唯一财务事实来源；
- 儿童保健、衣柜、消耗品和其他开支可以统一统计；
- 一笔付款可以拆分到多个模块；
- 所有 Allocation 合计等于 Transaction 金额；
- 模块记录与账本可以双向跳转；
- 免费、赠送和历史补录不会误记支出；
- 消耗品出库不会重复记账；
- 普通用户无需理解批次；
- 库存到期与自动出库仍然准确；
- 历史账本总额和月度汇总迁移前后一致；
- 健康数据默认私有；
- API Key、Cookie 和数据库密钥不会泄露；
- lint、类型检查、测试和生产构建全部通过；
- 数据迁移有备份、校验报告和回退方案。

## 20. 第一版范围

第一版必须包含：

- 家庭总账；
- 支出拆分；
- 儿童保健记录与开支；
- 衣柜与购衣开支；
- 消耗品库存与采购开支；
- 其他快速记账；
- 收入和退款；
- 统一登录；
- 家庭总览；
- 数据迁移；
- 旧 API 兼容。

第一版暂不包含：

- 复式记账；
- 多币种；
- 信用卡账单自动对账；
- 发票 OCR；
- 电商订单自动同步；
- 复杂预算；
- 单件衣物穿着次数；
- 多家庭完整管理 UI；
- 微服务和消息队列；
- 独立移动 App。

## 21. Git 与交付策略

建议分支：

```text
main
├── feature/platform-bootstrap
├── feature/ledger-core
├── feature/child-care
├── feature/wardrobe
├── feature/consumables
├── feature/composite-expense
└── feature/final-migration
```

规则：

- `main` 始终保持可构建；
- 每个阶段使用独立 PR；
- 数据库 Migration 与对应代码放在同一 PR；
- PR 必须包含测试结果、数据影响和回退方式；
- 不一次性复制两个旧项目后集中解决冲突；
- 不在没有备份的情况下修改生产数据。

## 22. 最终结论

平台的正确业务结构是：

```text
账本（核心）
├── 儿童保健开支
├── 衣柜开支
├── 消耗品开支
└── 其他收入与支出
```

技术结构是：

```text
一个 Next.js 项目
├── ledger 核心模块
├── care 业务模块
├── wardrobe 业务模块
├── consumables 业务模块
├── journal 辅助模块
├── 一套 Prisma 数据模型
├── 一个 Supabase PostgreSQL
└── 一个 Vercel 部署
```

实施顺序固定为：

```text
平台初始化
→ 账本核心
→ 儿童保健
→ 衣柜
→ 消耗品
→ 组合记账
→ 成长日志
→ 数据切换
```

账本先行，业务模块围绕账本扩展。钱只记一次，业务记录负责解释和追踪这笔钱。
