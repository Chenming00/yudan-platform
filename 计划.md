# 鱼蛋家庭开支平台建设计划

> 仓库：<https://github.com/Chenming00/yudan-platform>  
> 来源项目：
>
> - <https://github.com/Chenming00/yudan-baby-pantry>
> - <https://github.com/Chenming00/Yudan-log>
>
> 文档状态：架构与实施基线  
> 更新日期：2026-09-01

## 1. 产品定位

`yudan-platform` 是一个以“家庭开支账本”为核心的单项目、多模块家庭管理平台。

账本是整个平台的财务主线，所有真实发生的收入、支出、退款都统一进入账本。儿童保健、衣柜、消耗品不是与账本平级的孤立系统，而是产生或解释账本开支的业务模块。

```text
家庭开支账本
├── 儿童保健
│   ├── 儿保检查
│   ├── 疫苗
│   ├── 医疗 / 药品
│   └── 生长记录
├── 衣柜
│   ├── 衣物购买
│   ├── 尺码与季节
│   ├── 在穿 / 待穿 / 闲置
│   └── 单件成本
├── 消耗品
│   ├── 奶粉、尿不湿、湿巾等
│   ├── 采购入库
│   ├── 使用消耗
│   ├── 库存与补货
│   └── 到期提醒
└── 其他记账
    ├── 餐饮
    ├── 交通
    ├── 居家
    ├── 收入
    └── 自定义分类
```

核心原则：

> 钱的事实只在账本中保存一次；业务模块保存“这笔钱为什么发生、买了什么、产生了什么记录”。

## 2. 建设目标

平台最终实现：

1. 一个统一账本，汇总全部家庭收支；
2. 儿童保健开支与儿保、疫苗、医疗记录关联；
3. 衣柜开支与衣物、尺码、季节和使用状态关联；
4. 消耗品开支与采购、库存和补货关联；
5. 其他开支可以直接记账，不要求业务记录；
6. 一笔付款可以拆分到多个模块；
7. 一个模块中的业务记录可以追溯到账本；
8. 所有模块共享登录、数据库、UI、API 和部署；
9. `William.chen@utah.edu` 为平台超级管理员；
10. 关闭公开注册，新用户必须通过有效邀请码完成注册；
11. 已注册用户可以正常登录，不需要重复邀请码；
12. 保留旧项目数据，并提供可校验、可回退的迁移流程。

## 3. 总体架构

采用 Next.js 模块化单体：

- 一个 Git 仓库；
- 一个 Next.js 应用；
- 一个 Supabase PostgreSQL 数据库；
- 一套 Supabase Auth；
- 一套 Prisma 数据模型与 Migration；
- 一个 Vercel 部署单元；
- 多个边界清晰的业务模块。

当前阶段不采用微服务。家庭自用规模下，模块化单体可以保证跨模块数据库事务，同时避免额外的服务通信和运维成本。

技术栈：

| 层面 | 技术 |
|---|---|
| 全栈框架 | Next.js App Router |
| 前端 | React + TypeScript |
| UI | Tailwind CSS + Radix UI |
| 数据访问 | Prisma |
| 数据库 | Supabase PostgreSQL |
| 认证 | Supabase Auth + GitHub OAuth |
| 部署 | Vercel |
| 图表 | Recharts |
| 内容 | Markdown + Frontmatter |
| PWA | Web App Manifest |

### 3.1 部署平台决策

采用混合部署：

```text
用户
├── yudan-platform 主应用 → Vercel
│   ├── Next.js 页面与 Route Handler
│   ├── Supabase Auth / GitHub OAuth
│   ├── Prisma 业务逻辑
│   └── 权限、邀请和跨模块事务
├── 数据库 → Supabase PostgreSQL
└── 图片与附件 → Cloudflare
    ├── R2
    ├── CDN / 自定义资源域名
    └── Cloudflare Images（按需）
```

当前阶段选择 Vercel 作为主应用运行平台，原因：

- Next.js 在 Vercel 上是原生、低配置的部署路径；
- App Router、Route Handler、Server Action、SSR、ISR 和图片等功能不需要额外适配层；
- Git 分支和 Pull Request 可以自动生成 Preview Deployment；
- 当前项目使用 Prisma + Supabase PostgreSQL，使用 Vercel Node.js 运行时和 Supavisor transaction pooler 的改造较少；
- 邀请注册、权限系统、组合记账和跨模块事务更适合先在完整 Node.js 兼容环境中落地；
- 出现问题时构建日志、函数日志、Preview 和回滚链路更直接。

Cloudflare 当前可以通过 Workers 运行 Next.js，但新项目推荐路径是 vinext；vinext 仍需要兼容性检查，且会使用 Vite 重新实现 Next.js API 表面。OpenNext 适合作为已有 Cloudflare Next.js 项目的维护路径。把现有平台直接部署到 Cloudflare Workers，需要额外处理运行时兼容、适配器、Prisma Cloudflare runtime、`nodejs_compat` 或 Hyperdrive 等问题。

因此第一版不把完整 Next.js 应用部署到 Cloudflare Pages。Cloudflare Pages 只适合本项目未来可能产生的纯静态导出；全栈 Next.js 若迁往 Cloudflare，应部署到 Workers。

官方参考：

- <https://vercel.com/docs/frameworks/full-stack/nextjs>
- <https://developers.cloudflare.com/workers/framework-guides/web-apps/nextjs/>
- <https://developers.cloudflare.com/workers/framework-guides/web-apps/opennext/>
- <https://developers.cloudflare.com/hyperdrive/>
- <https://supabase.com/docs/guides/database/connecting-to-postgres>

主应用与资源层的连接：

```text
浏览器
├── 业务请求 → Vercel
├── 私有上传申请 → Vercel 权限校验
├── 文件直传 → Cloudflare R2 Presigned URL
└── 资源读取 → Cloudflare CDN / 短时签名 URL
```

Vercel 不代理大文件正文，只负责身份、权限、资源元数据和签名。这样可以降低函数带宽与执行时间，同时保持 R2 的直接分发能力。

数据库运行时连接使用适合 Serverless 的 Supabase 连接池；Prisma Migration、备份和管理操作使用独立连接与部署步骤，不在每次应用请求中执行。

迁移到 Cloudflare Workers 的条件：

1. Vercel 运行成本或区域延迟成为明确问题；
2. 应用需要大量使用 Workers、Queues、Durable Objects 或其他 Cloudflare Binding；
3. `vinext check` 对当前代码没有关键兼容缺口；
4. Prisma、Supabase Auth、OAuth Callback、R2 上传和全部端到端测试通过 Workers 预发布验证；
5. Vercel 与 Workers 双环境灰度完成；
6. 回滚方案经过演练。

为保留可迁移性：

- 业务逻辑不直接依赖 Vercel 专有 API；
- Cloudflare R2 通过 `MediaStorage` 接口封装；
- 数据库访问集中在 Repository；
- 环境变量通过统一配置模块读取；
- 登录回调域名不写死；
- 关键流程用端到端测试覆盖。

业务访问路径：

```text
页面 / API
    ↓
应用编排 Service
    ↓
账本 Service + 业务模块 Service
    ↓
各模块 Repository
    ↓
Prisma Transaction
    ↓
Supabase PostgreSQL
```

约束：

- 页面和 Route Handler 不直接调用 Prisma；
- 金额统一使用 Decimal；
- 账本金额不能在多个业务表中重复作为财务事实；
- 跨模块写入必须通过应用编排 Service；
- 涉及账本和业务记录的操作必须使用数据库事务；
- 模块不能直接修改其他模块的内部数据；
- 退款、撤销优先使用反向记录和状态变化，不直接删除历史。

## 4. 信息架构

桌面与移动端的主要入口：

```text
总览｜账本｜保健｜衣柜｜消耗品
```

次要入口：

- 成长日志；
- 数据导出；
- 分类管理；
- 支付账户；
- API 与自动化；
- 用户与权限；
- 系统设置。

页面规划：

| 路径 | 用途 |
|---|---|
| `/` | 家庭开支总览 |
| `/ledger` | 全部收入和支出 |
| `/ledger/new` | 快速记账 / 组合记账 |
| `/ledger/[id]` | 账目详情与业务关联 |
| `/care` | 儿童保健时间线和开支 |
| `/care/checkups` | 儿保检查 |
| `/care/vaccines` | 疫苗计划与接种 |
| `/care/growth` | 体重、身高、头围 |
| `/wardrobe` | 衣柜总览 |
| `/wardrobe/items` | 衣物列表 |
| `/wardrobe/new` | 购衣 / 录入衣物 |
| `/consumables` | 消耗品库存总览 |
| `/consumables/purchase` | 消耗品采购入库 |
| `/consumables/consume` | 消耗出库 |
| `/consumables/replenish` | 补货清单 |
| `/blog` | 成长日志 |
| `/settings` | 平台设置 |

总览首页重点展示：

- 本月总支出；
- 儿童保健支出；
- 衣柜支出；
- 消耗品支出；
- 其他支出；
- 最近账目；
- 待接种疫苗；
- 衣物尺码提醒；
- 低库存与临期提醒。

## 5. 推荐目录结构

```text
yudan-platform/
├── app/
│   ├── page.tsx
│   ├── ledger/
│   ├── care/
│   ├── wardrobe/
│   ├── consumables/
│   ├── blog/
│   ├── settings/
│   └── api/
│       ├── ledger/
│       ├── care/
│       ├── wardrobe/
│       ├── consumables/
│       └── automation/
├── modules/
│   ├── ledger/
│   │   ├── domain/
│   │   ├── services/
│   │   ├── repositories/
│   │   ├── schemas/
│   │   └── types.ts
│   ├── care/
│   ├── wardrobe/
│   ├── consumables/
│   ├── journal/
│   └── dashboard/
├── application/
│   ├── expense-orchestrator.ts
│   ├── refund-orchestrator.ts
│   └── import-orchestrator.ts
├── components/
│   ├── layout/
│   ├── navigation/
│   ├── charts/
│   └── ui/
├── lib/
│   ├── auth/
│   ├── db/
│   ├── api/
│   ├── date/
│   ├── money/
│   └── errors/
├── prisma/
│   ├── schema.prisma
│   ├── migrations/
│   └── seed.ts
├── content/
│   └── blog/
├── tests/
└── package.json
```

## 6. 账本核心模型

### 6.1 Transaction：钱的唯一事实

每次收入、支出或退款保存为一条 Transaction：

```prisma
model Transaction {
  id              String          @id @default(uuid())
  type            TransactionType
  amount          Decimal         @db.Decimal(12, 2)
  occurredAt      DateTime        @map("occurred_at")
  payee           String?
  paymentMethod   String?         @map("payment_method")
  account         String?
  note            String?
  status          TransactionStatus @default(POSTED)
  createdAt       DateTime        @default(now()) @map("created_at")
  updatedAt       DateTime        @updatedAt @map("updated_at")

  allocations     TransactionAllocation[]

  @@index([occurredAt])
  @@map("transactions")
}
```

建议类型：

```text
EXPENSE   支出
INCOME    收入
REFUND    退款
```

### 6.2 TransactionAllocation：账目拆分

一笔付款可能包含多个场景。例如一张电商订单同时购买尿不湿、衣服和药品，因此不能只在 Transaction 上保存一个分类。

```prisma
model TransactionAllocation {
  id            String       @id @default(uuid())
  transactionId String       @map("transaction_id")
  module        ExpenseModule
  category      String
  amount        Decimal      @db.Decimal(12, 2)
  sourceType    String?      @map("source_type")
  sourceId      String?      @map("source_id")
  note          String?
  createdAt     DateTime     @default(now()) @map("created_at")

  transaction Transaction @relation(fields: [transactionId], references: [id])

  @@index([transactionId])
  @@index([module, category])
  @@index([sourceType, sourceId])
  @@map("transaction_allocations")
}
```

建议模块枚举：

```text
CHILD_CARE    儿童保健
WARDROBE      衣柜
CONSUMABLES   消耗品
OTHER         其他
```

约束：

```text
Transaction.amount
=
SUM(TransactionAllocation.amount)
```

所有模块统计都基于 Allocation 聚合，家庭总账基于 Transaction 聚合。

### 6.3 为什么需要拆分

示例：

```text
京东付款 ¥520
├── 消耗品：尿不湿 ¥300
├── 衣柜：连体衣 ¥150
└── 儿童保健：维生素 ¥70
```

账本中只有一笔 ¥520 的付款，同时可以正确统计三个模块，且不会重复计算总支出。

## 7. 儿童保健模块

儿童保健模块包含两类数据：

1. 健康事实：体重、身高、头围、疫苗、儿保和就诊记录；
2. 财务关联：挂号、检查、疫苗、药品和保健品支出。

推荐结构：

```text
CareRecord
├── CHECKUP       儿保
├── VACCINE       疫苗
├── MEDICAL       就诊
├── MEDICINE      药品
└── SUPPLEMENT    保健品
```

保健记录可以没有支出，例如免费疫苗；也可以关联一个账本 Allocation，例如自费疫苗。

核心关系：

```text
Transaction
└── TransactionAllocation(module = CHILD_CARE)
       └── CareRecord
```

典型流程：

```text
记录自费疫苗
├── 接种日期
├── 疫苗计划
├── 医院
├── 金额
└── 自动生成儿童保健支出
```

免费疫苗：

```text
只创建接种记录，不创建支出
```

第一版儿童保健分类：

- 儿保；
- 疫苗；
- 挂号；
- 检查；
- 药品；
- 保健品；
- 其他医疗。

原 `Yudan-log` 的体重和疫苗数据迁移到此模块。

## 8. 衣柜模块

衣柜模块负责记录儿童衣物及其成本，不承担账本总额计算。

建议记录：

- 名称；
- 品类；
- 品牌；
- 尺码；
- 适用季节；
- 购买日期；
- 购入价格；
- 数量；
- 状态；
- 图片；
- 备注。

衣物状态：

```text
PLANNED     计划购买
UNWORN      待穿
ACTIVE      在穿
OUTGROWN    已小
STORED      收纳
DONATED     已赠送
DISCARDED   已丢弃
SOLD        已出售
```

核心关系：

```text
Transaction
└── TransactionAllocation(module = WARDROBE)
       └── WardrobePurchase
            └── WardrobeItem[]
```

典型流程：

```text
购买衣服
├── 创建一笔账本支出
├── 分类到衣柜
├── 创建一条购买记录
└── 批量生成衣物
```

赠送衣物只创建 WardrobeItem，不生成支出。二手出售应创建 INCOME 或 REFUND 类型账目，并与衣物关联。

第一版衣柜分类：

- 上装；
- 下装；
- 连体衣；
- 外套；
- 内衣；
- 鞋袜；
- 帽子；
- 睡袋；
- 配饰；
- 其他。

衣柜统计：

- 本月 / 本年购衣支出；
- 各尺码支出；
- 各品类数量；
- 在穿、待穿、已小数量；
- 单次穿着成本作为后续能力，不进入第一版。

## 9. 消耗品模块

消耗品模块从 `yudan-baby-pantry` 迁移，负责：

- 产品资料；
- 当前库存；
- 安全库存；
- 采购入库；
- 赠送和历史补录；
- 消耗出库；
- 临期和过期提醒；
- 补货清单；
- 库存日志。

第一版分类：

- 奶粉与辅食；
- 尿不湿；
- 湿巾与纸品；
- 洗护；
- 清洁；
- 喂养用品；
- 药品与护理耗材；
- 其他消耗品。

### 9.1 弱化批次

普通用户不操作“批次”，只看到产品当前库存。

系统内部保留 `StockEntry` 入库记录，用于：

- 不同到期日；
- 不同采购价格；
- 自动选择先到期库存；
- 撤销和审计；
- 采购与账本关联。

```text
Product
└── StockEntry[]
    ├── 入库数量
    ├── 剩余数量
    ├── 到期日期（可选）
    ├── 单位成本（可选）
    └── ConsumablePurchaseItem
```

UI 使用“入库记录”，不把批次编码作为主要信息。

### 9.2 与账本关联

```text
Transaction
└── TransactionAllocation(module = CONSUMABLES)
       └── ConsumablePurchase
            └── ConsumablePurchaseItem[]
                 └── StockEntry[]
```

采购时：

- 创建账本支出；
- 创建消耗品采购记录；
- 创建入库记录；
- 创建库存日志。

出库时：

- 只减少库存；
- 不再次产生账本支出；
- 消耗成本只作为统计，不计入第二次支出。

非采购入库来源：

```text
GIFT          赠送
TRANSFER      转入
ADJUSTMENT    盘点调整
HISTORICAL    历史补录
```

这些来源默认不产生账本支出。

## 10. 其他记账

其他记账是通用入口，不要求创建儿童保健、衣柜或消耗品记录。

示例分类：

- 餐饮；
- 交通；
- 居家；
- 娱乐；
- 教育；
- 人情；
- 工资与收入；
- 退款；
- 自定义分类。

流程：

```text
快速记账
├── 金额
├── 收入 / 支出 / 退款
├── 日期
├── 分类
├── 收款方 / 商户
├── 支付方式
└── 备注
```

创建：

- 一条 Transaction；
- 一条 module = OTHER 的 TransactionAllocation。

## 11. 组合记账与跨模块事务

平台必须支持一次付款包含多个模块。

组合记账页面：

```text
付款总额：¥520
商户：京东
日期：2026-09-01

明细：
1. 尿不湿      消耗品    ¥300
2. 连体衣      衣柜      ¥150
3. 维生素      儿童保健  ¥70
```

保存时在一个 Prisma 数据库事务中：

1. 创建 Transaction；
2. 创建三条 TransactionAllocation；
3. 创建消耗品采购和 StockEntry；
4. 创建衣柜购买记录和 WardrobeItem；
5. 创建保健品记录；
6. 创建必要的业务日志；
7. 校验明细金额总和；
8. 提交事务。

任一步失败，整笔操作回滚。

## 12. 修改、退款、撤销和删除

### 12.1 修改

未关联业务记录的普通账目可以直接修改。

已关联业务模块的账目必须通过对应业务流程修改，防止只改金额、不改采购或物品记录。

### 12.2 退款

退款创建新的 REFUND Transaction，不直接把原支出金额改小。退款 Allocation 关联原 Transaction 或原业务记录。

例如退掉一件衣服：

- 创建退款账目；
- 修改衣物状态；
- 保留原购买记录；
- 记录退款关联。

### 12.3 撤销

刚创建且没有后续操作的组合记账可以整体撤销。涉及已消耗库存、已出售衣物或后续健康记录时，不允许直接删除，必须使用冲销流程。

### 12.4 删除原则

财务记录和库存日志默认软删除或状态化，不做无痕物理删除。物理删除仅用于未正式提交的草稿和明确的测试数据。

## 13. 数据库表规划

### 账本核心

- `transactions`
- `transaction_allocations`
- `categories`
- `payment_accounts`

### 儿童保健

- `baby_profiles`
- `growth_records`
- `vaccine_catalog`
- `vaccine_records`
- `care_records`

### 衣柜

- `wardrobe_purchases`
- `wardrobe_items`
- `wardrobe_categories`

### 消耗品

- `products`
- `stock_entries`
- `inventory_logs`
- `consumable_purchases`
- `consumable_purchase_items`
- `product_groups`
- `product_group_items`

### 系统、用户与邀请

- `app_users`
- `households`
- `household_members`
- `roles`
- `permissions`
- `role_permissions`
- `user_global_roles`
- `invitations`
- `api_credentials`
- `idempotency_keys`
- `audit_logs`

Supabase Auth 保存身份、OAuth Provider 和 Session；`app_users` 保存平台用户状态；`household_members` 保存家庭角色；`invitations` 保存邀请码生命周期。第一阶段可以只有一个家庭空间，但所有主要业务表应预留 `household_id`，避免以后重做数据隔离。

## 14. Cloudflare 图片与资源存储

图片和附件统一存放在 Cloudflare，不把二进制文件写入 Supabase PostgreSQL、Git 仓库或 Vercel 文件系统。

### 14.1 产品选择

第一阶段采用：

- Cloudflare R2：原始图片、附件和导出文件的对象存储；
- Cloudflare CDN + 自定义域名：公开资源分发；
- 短时签名 URL 或鉴权资源网关：私有资源读取；
- Cloudflare Images：作为可选的图片裁剪、缩略图、格式优化能力。

R2 是文件事实来源。即使启用 Cloudflare Images，也建议把原图保存在 R2，Cloudflare Images 或 Worker 负责生成展示变体。

不使用 R2 SQL、D1 或 KV 保存业务数据。资源的业务归属、权限、状态和关联关系仍保存在 Supabase PostgreSQL。

### 14.2 Bucket 划分

建议至少使用两个 Bucket：

```text
yudan-assets-private
├── 儿童保健图片和附件
├── 衣柜照片
├── 消耗品照片
├── 账单凭证
├── 数据导出
└── 其他家庭私有资源

yudan-assets-public
├── Logo
├── PWA 图标
├── 成长日志公开图片
└── 明确允许公开的静态资源
```

私有 Bucket 默认不开放公共访问。公开与私有资源不能只靠目录名前缀隔离，避免错误的 Bucket 公共策略导致健康数据或账单凭证泄露。

建议公开资源使用自定义域名，例如：

```text
https://assets.ykn.cm/
```

具体域名在 Cloudflare 配置阶段确认。

### 14.3 对象 Key 设计

私有对象：

```text
households/{householdId}/{module}/{year}/{month}/{uuid}.{ext}
```

示例：

```text
households/hh_123/wardrobe/2026/09/550e8400-e29b-41d4-a716.webp
households/hh_123/care/2026/09/8f81a60e-4a2a-47b0-a912.pdf
```

公开对象：

```text
public/blog/{year}/{slug}/{uuid}.{ext}
public/brand/{uuid}.{ext}
```

规则：

- 对象 Key 由服务端生成；
- 不直接使用用户原始文件名；
- 禁止 `..`、绝对路径和用户可控路径片段；
- 原始文件名只作为数据库显示元数据保存；
- 对象 Key 不包含邮箱、宝宝姓名、医疗信息或其他隐私；
- 更新图片时生成新 Key，不覆盖旧对象，以便缓存和回退。

### 14.4 资源数据模型

推荐模型：

```prisma
model MediaAsset {
  id               String      @id @default(uuid())
  householdId      String?     @map("household_id")
  bucket           String
  objectKey        String      @unique @map("object_key")
  visibility       Visibility  @default(PRIVATE)
  originalFilename String?     @map("original_filename")
  contentType      String      @map("content_type")
  sizeBytes        BigInt      @map("size_bytes")
  sha256           String?
  width            Int?
  height           Int?
  status           MediaStatus @default(PENDING)
  uploadedBy       String      @map("uploaded_by")
  createdAt        DateTime    @default(now()) @map("created_at")
  confirmedAt      DateTime?   @map("confirmed_at")
  deletedAt        DateTime?   @map("deleted_at")

  links MediaLink[]

  @@index([householdId, status])
  @@map("media_assets")
}

model MediaLink {
  id           String @id @default(uuid())
  assetId      String @map("asset_id")
  module       String
  resourceType String @map("resource_type")
  resourceId   String @map("resource_id")
  purpose      String?

  asset MediaAsset @relation(fields: [assetId], references: [id])

  @@unique([assetId, module, resourceType, resourceId, purpose])
  @@index([module, resourceType, resourceId])
  @@map("media_links")
}
```

数据库保存稳定的 `asset_id` 和 `object_key`，不保存会过期的签名 URL。前端需要显示私有资源时，由服务端按权限生成短时访问地址。

建议状态：

```text
PENDING      已创建上传任务、尚未确认
READY        上传完成且校验通过
QUARANTINED  待检查或不允许展示
DELETED      逻辑删除、等待物理清理
```

### 14.5 上传流程

推荐客户端直传 R2，避免图片先经过 Vercel Serverless Function。

```text
客户端请求上传
        ↓
Next.js 服务端验证登录、权限、MIME、大小和业务归属
        ↓
创建 PENDING MediaAsset
        ↓
生成短时 Presigned PUT URL
        ↓
客户端直接上传到 R2
        ↓
客户端调用完成接口
        ↓
服务端 HEAD 校验对象大小、类型、ETag / Checksum
        ↓
MediaAsset 改为 READY
        ↓
创建 MediaLink
```

S3 SDK 连接 R2 时必须使用：

```text
region = auto
```

上传接口返回 `expiresAt`，客户端不得假设签名 URL 永久有效。

对于较大的附件可以使用 multipart upload；未完成的上传必须终止或由清理任务回收。第一版可以限制单文件大小，避免无必要的复杂上传流程。

### 14.6 文件校验

服务端维护允许列表：

- 图片：JPEG、PNG、WebP、AVIF；
- 文档：PDF；
- 是否允许 GIF、HEIC、SVG 根据实际需求单独决定。

SVG 可能包含脚本或外部引用，第一版建议不允许用户上传 SVG；品牌 SVG 只允许通过受控发布流程进入公开 Bucket。

校验要求：

- Content-Type；
- 文件扩展名；
- 文件头 / Magic Bytes；
- 文件大小；
- 图片尺寸；
- SHA-256（需要去重或完整性校验时）；
- 对象实际大小与客户端声明一致。

不能只信任浏览器提供的 MIME 和文件名。

### 14.7 访问与权限

资源权限继承业务资源权限：

```text
账单凭证      → ledger.read
儿童保健附件  → care.read
衣柜图片      → wardrobe.read
消耗品图片    → consumables.read
公开日志图片  → public
```

私有资源访问流程：

1. 验证 Session 或受限 API Key；
2. 检查 `app_users` 状态；
3. 检查资源所属 `household_id`；
4. 检查模块读取权限；
5. 检查 MediaLink 目标资源权限；
6. 返回短时签名 URL或由资源 Worker 流式返回；
7. 必要时记录敏感资源访问审计。

权限系统增加：

- `media.upload`
- `media.read`
- `media.delete`
- `media.public.manage`

`VIEWER` 默认可以读取有权访问的私有资源，但不能上传或删除。API Key 必须显式获得对应 Media Scope。

### 14.8 公开资源和缓存

公开资源通过 Cloudflare 自定义域名和 CDN 分发。

使用内容寻址或 UUID Key 后，可以设置长缓存：

```text
Cache-Control: public, max-age=31536000, immutable
ETag: 使用 R2 的 HTTP ETag
```

文件更新通过生成新 Key 实现，不依赖刷新同 URL 缓存。

私有资源不能使用公共 Bucket URL，不能设置公共永久缓存。私有响应建议使用短时签名和适合隐私数据的缓存策略。

CORS 只允许正式站点、明确的 Vercel Preview 和本地开发来源，不能为了省事把私有上传配置为 `*`。

### 14.9 Cloudflare Images

第一版优先完成 R2 上传、权限和原图分发。以下需求出现时启用 Cloudflare Images 或 Images Binding：

- 衣柜缩略图；
- 商品列表小图；
- 成长日志响应式图片；
- 自动 WebP / AVIF；
- 手机和桌面不同尺寸；
- 去除 EXIF 等不必要元数据。

推荐展示变体：

```text
thumbnail   200 × 200，cover
card        640 × 640，contain / scale-down
detail      最大宽度 1600，scale-down
```

保健证明、账单凭证等私密图片不能使用无签名的公开 Images URL。需要使用签名 URL或鉴权 Worker。

### 14.10 删除、生命周期和一致性

业务删除资源时：

1. 将 `MediaAsset` 标记为 `DELETED`；
2. 删除或停用 MediaLink；
3. 写入审计日志；
4. 后台任务延迟删除 R2 对象；
5. 删除失败可重试；
6. 成功后记录物理删除时间。

这样可以避免数据库事务成功但对象删除失败造成不可恢复的不一致。

生命周期建议：

- PENDING 且长期未确认的对象自动清理；
- 已删除对象保留短暂恢复窗口后物理删除；
- 数据导出文件设置明确到期策略；
- 日常频繁访问的图片使用 Standard；
- Infrequent Access 只用于确实低频且满足最短存储周期的归档；
- 不把 R2 Bucket 当作唯一数据库备份。

### 14.11 Cloudflare 凭据

建议独立凭据：

```text
R2 应用读写 Token
├── 仅目标 Bucket
└── Object Read & Write

R2 管理 Token
├── CORS
├── Lifecycle
└── Bucket 管理
```

管理 Token 不进入 Vercel 运行时。应用 Token 使用最小权限并保存于 Vercel Secret 环境变量。

建议环境变量：

```text
CLOUDFLARE_ACCOUNT_ID
R2_ACCESS_KEY_ID
R2_SECRET_ACCESS_KEY
R2_PRIVATE_BUCKET
R2_PUBLIC_BUCKET
R2_PUBLIC_BASE_URL
```

不得使用 `NEXT_PUBLIC_` 暴露任何 R2 Secret。

### 14.12 资源 API

- `POST /api/media/upload-intents`
- `POST /api/media/[id]/complete`
- `GET /api/media/[id]`
- `DELETE /api/media/[id]`
- `POST /api/media/[id]/links`
- `DELETE /api/media/[id]/links/[linkId]`

公开资源发布、变更可见性和删除应要求 `media.public.manage` 或 `SUPER_ADMIN`。

## 15. 注册、登录、邀请与权限

### 15.1 超级管理员

平台唯一引导超级管理员账号：

```text
William.chen@utah.edu
```

邮箱比较必须先去除首尾空格并转为小写。该邮箱通过 GitHub OAuth 登录后，服务端将对应 Supabase Auth User ID 绑定为 `SUPER_ADMIN`。

超级管理员权限：

- 创建、查看、撤销邀请码；
- 指定受邀用户角色；
- 管理用户启用、停用和角色；
- 管理家庭空间；
- 查看全平台数据；
- 管理分类、API Key、数据导入和迁移；
- 查看安全审计日志；
- 执行系统级配置。

邮箱只用于引导和恢复超级管理员身份。业务表、审计日志和外键必须使用稳定的 `auth.users.id`，不能把邮箱当作数据库主键。

超级管理员授权必须同时校验：

1. Supabase 返回的已验证邮箱；
2. GitHub OAuth Provider；
3. `app_users` 中的 `SUPER_ADMIN` 角色；
4. 用户状态为 `ACTIVE`。

不能只依赖浏览器提交的邮箱，也不能只依赖前端隐藏按钮。

### 15.2 注册与登录边界

系统关闭公开注册。

```text
未注册访客
├── 无邀请码：不能创建平台账号
└── 有有效邀请码：可以进入 GitHub OAuth 注册流程

已注册用户
└── 直接使用 GitHub 登录，不需要邀请码

被停用用户
└── 即使 GitHub 登录成功，也不能进入平台
```

“GitHub OAuth 成功”不等于“平台注册成功”。服务端回调必须检查该 Supabase 用户是否已经存在于 `app_users`：

- 已存在且状态为 `ACTIVE`：完成登录；
- 不存在：必须提交并消费有效邀请码；
- 状态为 `DISABLED`：拒绝登录；
- 超级管理员邮箱：允许执行首次引导注册。

### 15.3 邀请码模型

推荐模型：

```prisma
model Invitation {
  id             String           @id @default(uuid())
  tokenHash      String           @unique @map("token_hash")
  targetEmail    String?          @map("target_email")
  role           HouseholdRole
  householdId    String           @map("household_id")
  status         InvitationStatus @default(PENDING)
  expiresAt      DateTime         @map("expires_at")
  maxUses        Int              @default(1) @map("max_uses")
  usedCount      Int              @default(0) @map("used_count")
  createdBy      String           @map("created_by")
  usedBy         String?          @map("used_by")
  usedAt         DateTime?        @map("used_at")
  revokedAt      DateTime?        @map("revoked_at")
  createdAt      DateTime         @default(now()) @map("created_at")

  @@index([householdId, status])
  @@index([expiresAt])
  @@map("invitations")
}
```

邀请状态：

```text
PENDING    待使用
ACCEPTED   已使用
EXPIRED    已过期
REVOKED    已撤销
```

安全要求：

- 使用密码学安全随机数生成邀请码；
- 数据库只保存 SHA-256 或更强的令牌哈希；
- 明文邀请码只在创建时展示一次；
- 默认单次使用；
- 默认有效期建议 7 天；
- 可选绑定目标邮箱；
- 消费邀请码必须使用数据库事务和条件更新；
- 过期、撤销、已用或超过次数的邀请码必须拒绝；
- URL、日志、分析平台和错误报告不得长期保存明文邀请码；
- 注册完成后立即从浏览器地址栏移除邀请码；
- 邀请验证和 OAuth 回调需要限流。

### 15.4 邀请注册流程

```text
超级管理员创建邀请
        ↓
系统生成一次性邀请链接
        ↓
受邀用户打开 /invite/[token]
        ↓
服务端验证哈希、状态和有效期
        ↓
用户使用 GitHub OAuth
        ↓
OAuth Callback 再次验证邀请码
        ↓
创建 app_users
        ↓
创建 household_members
        ↓
原子消费邀请码
        ↓
建立登录 Session
```

创建平台用户、加入家庭和消费邀请码必须在同一个数据库事务中完成，避免邀请码已失效但用户已部分注册。

如果 OAuth 前后需要保存邀请上下文，应使用带签名、短时有效、HttpOnly、Secure、SameSite=Lax 的 Cookie，不能信任普通查询参数传回的角色和家庭 ID。

### 15.5 用户与角色

平台角色：

```text
SUPER_ADMIN   超级管理员
OWNER         家庭所有者
EDITOR        可编辑账本和业务数据
VIEWER        只读
```

建议用户状态：

```text
ACTIVE        正常
DISABLED      已停用
PENDING       待完成注册
```

权限以服务端 RBAC 为准：

| 操作 | SUPER_ADMIN | OWNER | EDITOR | VIEWER |
|---|---:|---:|---:|---:|
| 查看家庭数据 | ✓ | ✓ | ✓ | ✓ |
| 新增和编辑账目 | ✓ | ✓ | ✓ |  |
| 管理保健、衣柜、消耗品 | ✓ | ✓ | ✓ |  |
| 创建邀请码 | ✓ | 可选 |  |  |
| 管理成员角色 | ✓ | ✓ |  |  |
| 停用用户 | ✓ | 家庭范围 |  |  |
| 系统迁移和全量导出 | ✓ |  |  |  |

第一版建议仅允许 `SUPER_ADMIN` 创建邀请码；平台稳定后再决定是否开放给 `OWNER`。

### 15.6 权限系统设计

权限系统采用 RBAC（基于角色的访问控制）并叠加数据范围控制。

```text
用户
├── 全局角色
│   └── SUPER_ADMIN
└── 家庭成员身份
    ├── OWNER
    ├── EDITOR
    └── VIEWER
         ↓
       角色
         ↓
       权限码
         ↓
   household_id 数据范围
```

`SUPER_ADMIN` 是平台全局角色；`OWNER`、`EDITOR`、`VIEWER` 是特定家庭范围内的角色。同一个用户以后可以在不同家庭拥有不同角色。

推荐权限码：

#### 账本权限

- `ledger.read`
- `ledger.create`
- `ledger.update`
- `ledger.delete`
- `ledger.refund`
- `ledger.export`
- `ledger.category.manage`

#### 儿童保健权限

- `care.read`
- `care.create`
- `care.update`
- `care.delete`
- `care.export`

#### 衣柜权限

- `wardrobe.read`
- `wardrobe.create`
- `wardrobe.update`
- `wardrobe.delete`
- `wardrobe.export`

#### 消耗品权限

- `consumables.read`
- `consumables.create`
- `consumables.update`
- `consumables.consume`
- `consumables.adjust`
- `consumables.delete`
- `consumables.export`

#### 成员与邀请权限

- `member.read`
- `member.invite`
- `member.role.update`
- `member.disable`
- `invitation.read`
- `invitation.create`
- `invitation.revoke`

#### 系统权限

- `system.settings.read`
- `system.settings.update`
- `system.audit.read`
- `system.backup`
- `system.migration`
- `system.api_key.manage`

默认角色授权：

| 权限范围 | SUPER_ADMIN | OWNER | EDITOR | VIEWER |
|---|---:|---:|---:|---:|
| 所有模块读取 | ✓ | 家庭内 | 家庭内 | 家庭内 |
| 新增和编辑业务数据 | ✓ | 家庭内 | 家庭内 |  |
| 删除和退款 | ✓ | 家庭内 | 按配置 |  |
| 导出家庭数据 | ✓ | ✓ | 按配置 |  |
| 创建邀请 | ✓ | 第二阶段可开放 |  |  |
| 管理成员角色 | ✓ | 家庭内且不能提权到 SUPER_ADMIN |  |  |
| 查看审计日志 | ✓ | 家庭内 |  |  |
| 系统迁移、全量备份 | ✓ |  |  |  |
| 管理全局角色 | ✓ |  |  |  |

授权检查统一调用：

```ts
authorize({
  userId,
  householdId,
  permission: "ledger.update",
  resourceId,
});
```

授权流程：

1. 验证 Supabase Session；
2. 读取 `app_users` 并确认状态为 `ACTIVE`；
3. 检查全局角色；
4. 检查用户在目标 `household_id` 下的成员身份；
5. 解析角色对应权限码；
6. 检查目标资源是否属于同一个家庭；
7. 对敏感动作执行额外规则；
8. 记录必要的审计事件。

服务端分层：

- Middleware 只做“是否登录”和粗粒度路由保护；
- Route Handler / Server Action 检查权限码；
- Service 检查资源归属和业务状态；
- Repository 查询必须包含 `household_id`；
- 前端根据权限隐藏按钮，但前端状态不能作为授权依据。

权限缓存可以短时使用，但用户停用、角色变化、权限变化后必须主动失效。

### 15.7 权限数据模型

推荐模型：

```prisma
model Role {
  id          String   @id @default(uuid())
  code        String   @unique
  name        String
  scope       RoleScope
  isSystem    Boolean  @default(true) @map("is_system")

  permissions RolePermission[]

  @@map("roles")
}

model Permission {
  id          String   @id @default(uuid())
  code        String   @unique
  description String?

  roles RolePermission[]

  @@map("permissions")
}

model RolePermission {
  roleId       String @map("role_id")
  permissionId String @map("permission_id")

  role       Role       @relation(fields: [roleId], references: [id])
  permission Permission @relation(fields: [permissionId], references: [id])

  @@id([roleId, permissionId])
  @@map("role_permissions")
}

model HouseholdMember {
  id          String       @id @default(uuid())
  householdId String       @map("household_id")
  userId      String       @map("user_id")
  roleId      String       @map("role_id")
  status      MemberStatus @default(ACTIVE)

  role Role @relation(fields: [roleId], references: [id])

  @@unique([householdId, userId])
  @@index([userId, status])
  @@map("household_members")
}

model UserGlobalRole {
  userId String @map("user_id")
  roleId String @map("role_id")

  role Role @relation(fields: [roleId], references: [id])

  @@id([userId, roleId])
  @@map("user_global_roles")
}
```

系统角色和权限由 Migration 或 Seed 创建。生产环境不允许客户端自行创建 `SUPER_ADMIN` 角色绑定。

### 15.8 数据范围与越权防护

所有账本、保健、衣柜、消耗品和日志记录必须保存 `household_id`。

普通成员的所有查询必须同时满足：

```text
记录.household_id
=
当前授权上下文.household_id
```

禁止：

- 根据前端传入的 `household_id` 直接授权；
- OWNER 把自己或他人提升为 `SUPER_ADMIN`；
- 用户修改自己的全局角色；
- 邀请人授予高于自身的角色；
- API Key 跨家庭读取；
- 仅凭资源 ID 查询后再做前端过滤；
- 通过导出接口绕过模块读取权限。

超级管理员跨家庭访问敏感数据时仍应写入审计日志，包括访问原因、目标家庭、资源和时间。

### 15.9 API Key 权限

API Key 使用独立凭据模型，不默认继承创建者全部权限。

每个凭据包含：

- 名称；
- token 哈希；
- 所属家庭；
- 权限码集合；
- 过期时间；
- 最后使用时间；
- 启用状态；
- 创建者。

例如记账机器人只授予：

```text
ledger.read
ledger.create
```

库存 Agent 可以授予：

```text
consumables.read
consumables.create
consumables.consume
```

API Key 不能拥有 `system.migration`、`member.role.update` 或创建超级管理员的能力。

### 15.10 审计日志

以下动作必须记录：

- 登录成功和失败；
- 邀请创建、接受、过期和撤销；
- 用户启用、停用；
- 角色和权限变化；
- 账本删除、退款和导出；
- 健康数据导出；
- 库存盘点和强制调整；
- API Key 创建、轮换和撤销；
- 超级管理员跨家庭访问；
- 数据备份和 Migration。

审计日志至少包含：

```text
actor_user_id
household_id
action
resource_type
resource_id
result
ip_hash
user_agent
metadata
created_at
```

审计日志不得保存邀请码、API Key、Cookie、医疗备注全文或其他不必要的敏感信息。

### 15.11 会话和安全原则

- Supabase Auth 负责 GitHub OAuth 和 Session；
- OAuth Callback 只允许配置的正式域名、预览域名和本地开发地址；
- 所有受保护页面在服务端检查 Session 和平台用户状态；
- Prisma 和数据库密钥只在服务端使用；
- API Key 不使用 `NEXT_PUBLIC_`；
- Cookie 写请求检查 Origin；
- API Key 使用常量时间比较；
- 登录、邀请码验证和管理接口必须限流；
- 邀请创建、撤销、接受、角色变化和用户停用必须写入 `audit_logs`；
- 日志禁止记录完整邀请码、API Key、Cookie 和数据库连接串；
- 健康、账本和衣柜数据默认仅家庭成员可见；
- 写接口支持幂等键；
- 数据迁移前必须备份；
- 生产数据库只使用正式 Prisma Migration。

## 16. API 规划

### 认证、用户与邀请

- `GET /api/auth/me`
- `POST /api/auth/logout`
- `GET /api/invitations/validate`
- `POST /api/admin/invitations`
- `GET /api/admin/invitations`
- `POST /api/admin/invitations/[id]/revoke`
- `GET /api/admin/users`
- `PATCH /api/admin/users/[id]`
- `GET /api/admin/roles`
- `GET /api/admin/permissions`
- `PATCH /api/admin/roles/[id]/permissions`
- `GET /api/admin/audit-logs`
- `POST /api/admin/api-credentials`
- `POST /api/admin/api-credentials/[id]/revoke`

邀请码验证接口只能返回必要状态，不返回 token 哈希、创建者敏感信息或可用于枚举用户的数据。

### 账本

- `GET /api/ledger/transactions`
- `POST /api/ledger/transactions`
- `POST /api/ledger/composite`
- `PATCH /api/ledger/transactions/[id]`
- `POST /api/ledger/transactions/[id]/refund`
- `GET /api/ledger/summary`

### 儿童保健

- `GET /api/care/records`
- `POST /api/care/records`
- `GET /api/care/growth`
- `POST /api/care/growth`
- `GET /api/care/vaccines`
- `POST /api/care/vaccines`

### 衣柜

- `GET /api/wardrobe/items`
- `POST /api/wardrobe/purchases`
- `PATCH /api/wardrobe/items/[id]`
- `POST /api/wardrobe/items/[id]/sell`

### 消耗品

- `GET /api/consumables/products`
- `POST /api/consumables/purchases`
- `POST /api/consumables/consume`
- `POST /api/consumables/adjust`
- `GET /api/consumables/replenish`

旧 API 在迁移期保留兼容层，特别是 Telegram、Agent Skill 和现有脚本正在使用的接口。

## 17. 数据迁移计划

### 17.1 账本

原 `Yudan-log.transactions` 迁移为：

- 一条 Transaction；
- 一条 TransactionAllocation；
- 可识别的宝宝用品类别映射到对应模块；
- 无法可靠识别的历史记录统一映射为 OTHER；
- 不根据备注强行猜测具体业务对象。

校验：

- 交易数量一致；
- 收入总额一致；
- 支出总额一致；
- 退款总额一致；
- 每月统计一致；
- 时区显示一致。

### 17.2 儿童保健

```text
yudan_dashboards       → baby_profiles
yudan_weight_records   → growth_records
yudan_vaccine_catalog  → vaccine_catalog
yudan_vaccine_records  → vaccine_records
```

历史健康记录不强制生成支出。只有能够明确匹配的历史账目才建立关联。

### 17.3 消耗品

```text
Product                    → Product
InventoryBatch             → StockEntry
InventoryLog               → InventoryLog
ProductGroup               → ProductGroup
ProductGroupItem           → ProductGroupItem
```

原批次编码保存在 `legacy_batch_code`，新 UI 不突出批次。

历史库存采购价格可以形成迁移后的消耗品采购信息，但不能凭价格字段自动生成账本支出，避免与现有历史账目重复。

### 17.4 衣柜

现有两个来源项目没有完整衣柜数据模型，因此衣柜作为新模块建设。若现有账本中包含购衣记录，可以分类迁移，但不自动生成具体 WardrobeItem，除非用户后续补录。

### 17.5 成长日志

保留 Markdown、Frontmatter、slug、日期、标签、静态资源和旧 URL。

## 18. 分阶段实施计划

### 阶段 0：平台初始化

- 初始化 Next.js、TypeScript、Tailwind、Radix UI；
- 配置 Prisma、Supabase Auth 和 Vercel；
- 配置 Vercel Git Preview、Production 和回滚流程；
- 配置 Supabase Serverless 运行时连接与独立 Migration 连接；
- 建立 `app_users`、`household_members` 和 `invitations`；
- 创建 Cloudflare R2 公共与私有 Bucket；
- 配置上传 CORS、自定义资源域名和最小权限 Token；
- 建立 MediaAsset、MediaLink 和资源权限接口；
- 实现超级管理员首次引导；
- 实现邀请码注册、GitHub 登录、用户停用和服务端 RBAC；
- 建立角色、权限码、家庭数据范围、API Key Scope 和审计日志；
- 建立模块目录、基础布局和移动导航；
- 配置 lint、类型检查、测试和 CI；
- 建立环境变量示例。

完成标准：

- lint、类型检查和生产构建通过；
- Vercel Preview 可访问；
- Cloudflare R2 上传和资源访问可在 Preview 环境验证；
- 超级管理员可以登录；
- 无邀请码的新用户不能注册；
- 有效邀请码可以完成 GitHub OAuth 注册；
- 已注册用户可以直接再次登录；
- 被停用用户不能进入平台；
- OWNER、EDITOR、VIEWER 的权限矩阵测试通过；
- 跨家庭资源访问被服务端拒绝；
- API Key 只能调用被授予的权限；
- 私有图片只能由有权家庭成员访问；
- 客户端可以使用短时上传 URL 直传 R2；
- 上传完成后对象元数据校验通过；
- 公开资源自定义域名可以正常缓存。

### 阶段 1：账本核心

- 建立 Transaction 和 TransactionAllocation；
- 迁移账本列表、增删改和统计；
- 建立分类与支付方式；
- 建立快速记账；
- 保留旧账本 API 兼容层；
- 迁移历史账目并完成金额校验。

完成标准：

- 总账和月度统计与旧系统一致；
- 支持 CHILD_CARE、WARDROBE、CONSUMABLES、OTHER；
- 支持一笔账拆分多个模块；
- 金额全部使用 Decimal。

### 阶段 2：儿童保健

- 迁移宝宝资料、体重和疫苗；
- 建立儿保、就诊、药品和保健品记录；
- 实现免费记录和付费记录；
- 实现保健记录与账本双向跳转；
- 配置健康数据权限。

完成标准：

- 历史健康数据一致；
- 自费记录可以同步记账；
- 免费疫苗不会生成支出；
- 未授权用户无法读取真实数据。

### 阶段 3：衣柜

- 建立衣物、购买记录、尺码、季节和状态；
- 实现购衣自动记账；
- 支持赠送录入；
- 支持闲置、赠送、丢弃、出售和退款；
- 建立衣柜统计。

完成标准：

- 购买衣物可同时进入账本和衣柜；
- 赠送衣物不产生支出；
- 出售和退款不会改写历史购买事实。

### 阶段 4：消耗品

- 迁移 Pantry 的产品、库存和日志；
- 将批次 UI 改为入库记录；
- 保留先到期先出、补货和临期提醒；
- 实现消耗品采购自动记账；
- 实现非采购入库；
- 迁移 PWA、产品组和外部 API。

完成标准：

- 迁移前后库存一致；
- 用户无需手动选择批次；
- 采购同时产生支出和库存；
- 消耗只减库存、不重复记账。

### 阶段 5：组合记账与总览

- 建立一次付款多模块明细；
- 实现跨模块数据库事务；
- 建立家庭总览和模块对比；
- 增加退款与冲销；
- 增加幂等保护和审计日志。

完成标准：

- 一笔付款可同时创建保健、衣柜和消耗品记录；
- 明细合计必须等于付款总额；
- 任一步失败整笔回滚；
- 总览不存在重复支出。

### 阶段 6：成长日志与正式切换

- 迁移成长日志；
- 完成旧域名和旧 API 跳转；
- 完成生产数据最终迁移；
- 更新文档；
- 观察运行一至两周；
- 原项目转为只读或归档。

## 19. 测试重点

单元测试：

- Decimal 金额计算；
- Allocation 合计；
- 模块分类统计；
- 库存聚合与出库顺序；
- 退款金额；
- 疫苗建议日期；
- 衣物状态变化；
- 权限判断。

集成测试：

- 未授权用户不能生成上传 URL；
- 上传 URL 过期后不能继续使用；
- 上传完成接口能拒绝类型或大小不匹配对象；
- 跨家庭用户不能读取私有资源；
- 删除资源后数据库与 R2 清理任务状态一致；
- 公开资源返回正确 Cache-Control 和 ETag；
- 普通记账只创建账本数据；
- 自费疫苗同时创建保健记录和支出；
- 免费疫苗不创建支出；
- 购衣同时创建账本、购买记录和衣物；
- 消耗品采购同时创建账本和库存；
- 消耗出库不创建第二笔支出；
- 组合记账任一步失败时全部回滚；
- 幂等重试不会重复记账或重复入库；
- 退款保留原始交易并正确冲减统计。

端到端测试：

- 超级管理员首次登录与角色绑定；
- 无邀请码注册被拒绝；
- 有效邀请码完成 GitHub OAuth 注册；
- 过期、撤销和重复使用的邀请码被拒绝；
- 已注册用户无需邀请码再次登录；
- 被停用用户登录后无法进入平台；
- VIEWER 不能通过直接调用 API 写入数据；
- EDITOR 不能管理成员或系统设置；
- OWNER 不能授予 SUPER_ADMIN；
- 不同家庭之间不能通过资源 ID 越权访问；
- 受限 API Key 不能调用未授权接口；
- GitHub 登录；
- 手机端快速记账；
- 组合记账；
- 儿保和疫苗录入；
- 购衣和衣物状态管理；
- 消耗品采购、出库和补货；
- 月度统计；
- API Key 调用；
- PWA 安装与离线状态提示。

## 20. 验收标准

发布前必须满足：

- 账本是全平台唯一财务事实来源；
- 儿童保健、衣柜、消耗品和其他开支可以统一统计；
- 一笔付款可以拆分到多个模块；
- 所有 Allocation 合计等于 Transaction 金额；
- 模块记录与账本可以双向跳转；
- 免费、赠送和历史补录不会误记支出；
- 消耗品出库不会重复记账；
- 普通用户无需理解批次；
- 库存到期与自动出库仍然准确；
- 历史账本总额和月度汇总迁移前后一致；
- 健康数据默认私有；
- API Key、Cookie 和数据库密钥不会泄露；
- lint、类型检查、测试和生产构建全部通过；
- 数据迁移有备份、校验报告和回退方案。

## 21. 第一版范围

第一版必须包含：

- 家庭总账；
- 支出拆分；
- 儿童保健记录与开支；
- 衣柜与购衣开支；
- 消耗品库存与采购开支；
- 其他快速记账；
- 收入和退款；
- 超级管理员、邀请码注册和统一登录；
- 家庭总览；
- 数据迁移；
- 旧 API 兼容。

第一版暂不包含：

- 复式记账；
- 多币种；
- 信用卡账单自动对账；
- 发票 OCR；
- 电商订单自动同步；
- 复杂预算；
- 单件衣物穿着次数；
- 多家庭完整管理 UI；
- 微服务和消息队列；
- 独立移动 App；
- 视频转码和直播；
- 全量历史图片自动迁入 Cloudflare Images。

## 22. Git 与交付策略

建议分支：

```text
main
├── feature/platform-bootstrap
├── feature/ledger-core
├── feature/child-care
├── feature/wardrobe
├── feature/consumables
├── feature/composite-expense
└── feature/final-migration
```

规则：

- `main` 始终保持可构建；
- 每个阶段使用独立 PR；
- 数据库 Migration 与对应代码放在同一 PR；
- PR 必须包含测试结果、数据影响和回退方式；
- 不一次性复制两个旧项目后集中解决冲突；
- 不在没有备份的情况下修改生产数据。

## 23. 最终结论

平台的正确业务结构是：

```text
账本（核心）
├── 儿童保健开支
├── 衣柜开支
├── 消耗品开支
└── 其他收入与支出
```

技术结构是：

```text
一个 Next.js 项目
├── ledger 核心模块
├── care 业务模块
├── wardrobe 业务模块
├── consumables 业务模块
├── journal 辅助模块
├── 一套 Prisma 数据模型
├── 一个 Supabase PostgreSQL
└── 一个 Vercel 部署
```

实施顺序固定为：

```text
平台初始化
→ 账本核心
→ 儿童保健
→ 衣柜
→ 消耗品
→ 组合记账
→ 成长日志
→ 数据切换
```

账本先行，业务模块围绕账本扩展。钱只记一次，业务记录负责解释和追踪这笔钱。
